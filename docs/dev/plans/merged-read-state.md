# Merged read state (one definition across a merge group)

## Goal

Make a chapter count as **read, bookmarked or downloaded when any member source of a merge group says so**, and make every surface agree on that. Today the answer changes depending on which screen asks, so a merged series can show unread chapters it does not have, resume into a chapter already read on another source, and offer to re-download a file that is already on disk.

## Why

A merge group presents several sources as one series, but per-chapter state stays per-source row. Both chapter aggregators keep exactly one row per cross-source key and discard the losing duplicates, so the surviving row's `read` / `bookmark` / `downloaded` fields become the display state and the other sources' copies become invisible. Nothing merges them.

That produces contradictions the user meets directly:

- The library unread badge is the collapse representative's own count, deliberately not a sum (summing double-counts shared chapters), so it under-reports the group.
- The details "All" list shows a chapter as unread when it was read on a source that lost the dedup.
- Continue-reading, download-next, reader next-chapter and history resume all compute "next unread" from a per-manga query that never sees the group, so they can serve a chapter already read elsewhere.
- The library badge already sums downloads across members while the chapter row reads only the winning copy, so the badge and the list disagree today, before any of this changes.

Fixing only the visible surfaces would make it worse, not better: a badge reading zero unread while continue-reading still opens a read chapter is a sharper contradiction than the current consistent under-reporting.

## Approach

**One shared "any member" resolution, computed where the losers still exist.** Both aggregators already hold every member's chapters at the moment they dedup; that is the only point where the discarded copies are still in hand. Resolve the merged state there and carry it forward.

**Carry it on the UI item, never on the domain model.** `Chapter` is the shared domain and DB model, and several consumers depend on it being DB truth (mark-as-unread, tracker sync, delete-after-read, backup). Synthesising `read = true` onto it would leak into all of them. The merged flags belong on the per-screen chapter item the details list already builds.

**One definition of "the same chapter", and it is the stitch's.** Manga matches on the chapter number narrowed to `Float`; novels match on a normalized title string, falling back to the number. Neither key is the whole answer, because the stitch also places a chapter that no key identifies, by its position between chapters that do. So the identity is whatever the stitch decided, and everything else reads that rather than restating it. The shared helper beside the aggregators takes the key function as its input, so manga and novel cannot drift into two definitions.

**Teach the next-unread family about groups.** The details resume button already runs over the aggregated list and agrees for free. The other four (library continue-reading, library download-next, reader next-chapter plus its download-next, history resume) each call a per-manga interactor and need the group resolved first.

**Group-aware download means the deduplicated list, never a fan-out.** Downloading every member would fetch each chapter once per source and waste the storage on near-duplicates, so the target is one row per canonical chapter, downloaded from its own source. The details screen already does exactly this, acting on the visible list and grouping by each chapter's `mangaId`. Library download-next resolves the same list, and skips a chapter any member already holds on disk.

**The unread count moves as a set.** The badge, the unread filter predicate and the unread sort all read the same number. They change together or the library contradicts itself.

**The count is cached as a mapping, never as a count.** Read status is not an input to either aggregator, so reading can never change which chapters are duplicates, but it changes the count constantly. `merged_chapter_unit` (with a novel twin) holds the stitch's own output per merge group: every member chapter, the merged chapter it belongs to, and where it ranks among the sources holding that chapter. The count is a live SQL join over that and the `read` column, the same cost class as the aggregates `libraryView` already computes. Members that have left the library and excluded scanlators are filtered at read time rather than before the stitch, so neither forces a rebuild and both stay instant.

**Scope:** read, bookmark and downloaded state, in one pass, since all three come from the identical discard and splitting them would mean touching the same code three times.

## Key files

- The stitch and the match keys: `reikai/domain/manga/ChapterAggregation.kt` (`Float` chapter number), `reikai/domain/novel/NovelChapterAggregation.kt` (`matchKey`, normalized title), both over the one ordering kernel `reikai/domain/merge/MergedChapterOrder.kt` and the one ranking rule `reikai/domain/merge/SourceRanking.kt`.
- Where the stitch runs and is kept current: `MangaGroupStitcher` / `NovelGroupStitcher`, driven only by `reikai/domain/merge/ReconcileMergedChapters.kt`.
- Where every surface reads it: `reikai/domain/manga/MergedChapterProvider.kt` and `reikai/domain/novel/NovelMergedChapterProvider.kt` (`stitchOf`), and the read-side kernels in `reikai/domain/merge/StoredStitch.kt` (`renderStoredStitch`, `flaggedOnAnotherSource`, `expandToUnits`). `NovelDetailsViewModel` only calls `rankedMemberIds`, to order the Manage sources dialog.
- Details list item to widen: the chapter item model in `MangaViewModel`, and the novel twin on `EntryDetailsScreenState`.
- Next-unread callers: `LibraryViewModel` (`getNextUnreadChapter`, `downloadNextChapters`), `ReaderViewModel` (`GetNextChapters`), `HistoryViewModel`.
- Unread count set: the badge in `MangaMergeCollapse` / `NovelMergeCollapse`, the filter predicate and the sort in both library models.
- Persistence: `data/src/main/sqldelight/tachiyomi/data/merged_chapter_unit.sq` (the stored stitch, the ranking it was built under, and the staleness views), created by migrations 42 and 43; the ranking inputs are read from `merge_group.sq`.

## Status

**Shipped.** Pulled forward ahead of the rest of the library surface at the owner's request, so it landed before the shared pipeline rather than after.

Shipped and on-device verified: the `chapter_match_key` schema and migration 31 (`004077165`), the reconciliation that computes and maintains the identities plus the shared key rules (`8a88a6109`), the update-job triggers and the `185f` backfill migration with `versionCode` 185 (`cb37e887e`), the deduplicated count wired into both library models with the novel collapse-before-filter reorder (`7fba8e5f2`), the details "All" list read flag (`116f74ca5`), source chips reporting group read state (`e45bdf483`), the reader chapter sheet keeping read chapters and back-navigation reaching them (`508bf14ff`), group-wide read/bookmark from the sheet (`82aebf3cb`), and the details `expandToGroup` matching on the same Float key (`d1f0dafb8`).

**The per-chapter match-key tables were replaced by the stored stitch (2026-09-08).** `chapter_match_key` and its novel twin derived a key from one chapter row with no group in scope, and the count queries over them restated the stitch's rules in SQL. They could not express the rules: a sibling's unidentifiable chapter is dropped by the stitch and was counted by the query, and a chapter placed by position between shared titles has no per-chapter key at all. The library badge and the chapter list therefore reached different totals for one series, measured at 550 against 541 on a two-source novel. `merged_chapter_unit` replaces both tables, `ReconcileMergedChapters` replaces `ReconcileChapterMatchKeys`, and `MergedCountConformanceTest` runs the stitch and the badge over one fixture and asserts they agree, so a second definition cannot be written again without a red test. Migration 42 creates the new tables and drops the old ones; both were rebuildable caches, so nothing of the user's is lost, and the restore path now stitches too rather than leaving a restored group unbadged until the next library update.

**Every surface now reads that stitch instead of running its own (2026-09-08).** Stitching happens in exactly two places, `MangaGroupStitcher` and `NovelGroupStitcher`, both only from the reconciliation. The details lists, both novel readers, the manga reader's group load, next-unread and `expandToGroup` all read `merged_chapter_unit` through a provider that rebuilds a stale group first, so a screen opening on chapters that just synced still gets current numbers. Three things fall out. `expandToGroup` stopped matching raw chapter numbers, which two sources count differently, so a bulk action reached a chapter several along on the sibling source. The novel details list gained the cross-source read flag manga already had. And the per-chapter match key is now used for nothing but its own gallery-source predicate, since identity comes from the stitch.

**The two surfaces disagree about what a merged selection means (recorded 2026-08-12).** A bulk action in the library expands a selected merged row to every source behind it, so marking one row read marks the whole group; the recents surface does the opposite, because its collapse drops the other members' rows outright, leaving the verbs to act on the one chapter the surviving row names. Both are defensible where they stand (the library selects series, recents selects chapters), and closing the gap means giving recents the cross-source chapter identity the library already uses, which is this record's remaining work rather than a defect of either surface.

**The surfaces outside the library are finished (2026-09-09).** What the earlier note called "still open" was partly already done: reader next-chapter and history resume had both become group-aware when the reader and recents takeovers landed, and the note had gone stale. What was actually left, and what this pass did:

- **Download-next reads the group's deduplicated list**, in `LibraryViewModel.downloadNextChapters` and `NovelLibraryViewModel.performDownloadAction`, each chapter fetched from the source that carries it (grouped by owner, since `DownloadManager.downloadChapters` takes the owning entry and writes into its folder). It skips a chapter any member already holds on disk, which is what the old per-source exclusion could not see. `ReaderViewModel.downloadNextChapters` moved onto the reader's own cross-source list for the same reason; the novel readers already walked theirs.
- **Bookmarked and downloaded are read back across the group**, through the same kernel read state uses: `readOnAnotherSource` became `flaggedOnAnotherSource`, taking the flag as a predicate, so read, bookmark and downloaded cannot become three rules. Both details lists, both sets of chapter filters and the two download-action selectors read it. Deleting a download expands to the group's copies, since the row reads as downloaded when any of them holds the file.
- **The recents verbs expand to the group**, matching the details screen: mark-read, bookmark and delete-download reach every source's copy, download does not (the members carry the same chapter, so downloading each copy fetches it once per source). `RecentsChapterActions` became suspend to allow the stitch read, and each adapter moves that read off the main thread itself.
- **Two ordering leftovers went with it.** `MangaRecentsAdapter.readingOrder` still sorted a merged list by `chapterNumber`, so recents and the library could name different "first unread" chapters for one series; it reads the stitched position now. The reader's downloaded-only filter (`filterDownloaded`) probed every chapter against the opened manga, reporting a sibling source's chapters as missing.
- **`crossSourceReadIds` is deleted, with the `CrossSourceReadState.kt` that held it.** It was the pre-stitch definition of read-elsewhere, with no production caller left; the conformance test now runs the real kernel over the stitch's own units.

**A ten-agent audit of that pass found six more sites and two regressions (2026-09-09).** Recorded because they are the shape this defect keeps taking, not because the list is interesting. The regressions were both the chip view: a source chip narrows the screen's per-source manga map to the one chip, so an expanded delete resolved a third source's copy to the anchor and deleted from the wrong folder, and `Delete downloads` cleared only the chip's source. Owners now resolve from the group (`MangaViewModel.ownerOf`, `groupManga`). The six: `removeDuplicateChapters` collapsed a merged list by chapter number in all three readers, which the stitch has already decided and two sources routinely disagree about, so it takes an `ownerOf` and dedupes within one entry; the reader's bookmarked and downloaded filters stayed per-copy while read went group-wide; `ChapterFilter.applyFilters` restated the details filter with the old flags and probed every chapter against the opened manga, which the library's resume button reads through; the manga reader's skip-read read the group's set, whose ids name the merged list's rows and never a source-scoped session's; and reader download-ahead walked the display-filtered list, so in downloaded-only mode it had nothing to queue. The recents rows now report the group's read and bookmarked state too.

**Still open:** whether the Updates feed should hide a sibling's copy of an already-read chapter is undecided. Two things are deliberately left, each with its reason:

- A merged chapter whose file sits on a source the stitch does not show **still streams when opened**. Reading the sibling's copy would mean opening a different chapter row, with its own history record and position.
- The **recents row's download indicator** answers for its own copy. Its state is a lambda the row evaluates as it draws, so asking it of every copy multiplies a disk probe by the group size on each recomposition, and the Delete it offers would then have to route to the copy holding the file.

**The three surfaces that still counted per member are done (2026-09-09).** An update run now counts, announces and downloads one copy per merged chapter (`collapseNewChapters`, run after the reconciliation so the stitch already covers what just arrived): a chapter is news only when the group did not already have it, and of the copies that arrive together the stitch's first-ranked one stands for them. Both jobs hold their downloads until that collapse rather than queueing inside the per-entry loop, which is what let two sources fetch the same file. The download badge counts merged chapters with a copy on disk rather than summing the members (`downloadedUnitsByGroup` over `downloadUnitsByGroup`), and the widget draws one cover per group. The badge's cost, which this record had rejected it over, is one indexed query plus a probe of only those members that have downloaded something, so a library whose merged entries hold nothing pays the query alone.

**Two rules the update collapse had to keep apart.** Which chapter is *announced* is the stitch's call: the first-ranked copy of a merged chapter the group did not already have. Which chapter is *downloaded* is not, because eligibility is decided per entry, against that entry's categories and its own read chapters, so the copy that may be downloaded is often not the copy announced. Intersecting the two left a merged chapter downloading from nowhere, and the collapse therefore hands out a deduplication key rather than an answer: the download half keeps one copy per merged chapter among the copies its own rules already allowed. A chapter the stitch places nowhere is announced rather than dropped, since the Updates feed lists it whatever the stitch decided.

**A run that does not finish still queues and still counts, uncollapsed.** Its chapters are written and are never new again, so dropping them loses them; queueing a duplicate on that path is the cheaper mistake. Both jobs do that from a `finally`, which is also what keeps a cancelled run's arrivals on the Updates badge.

**Known gap:** the widget refreshes on new chapters, not on merges, so a just-merged series keeps two covers until its next chapter arrives.

**The twins this pass left unpinned**, each with the rung that would pin it, to be paid the next time either half is touched: the two `groupChapterIds` helpers and the two `expandToGroup` helpers (hoist the plumbing to one suspend function over an id-fetch lambda, then one `@ParameterizedTest` over both entry types); the "skip what the group already holds on disk" rule in the two library download paths (extract the target choice as a pure function, which is also the only way either half becomes testable, since neither ViewModel has a test harness); and "the merged list runs newest-first", asserted in two comments and encoded twice, which `MergedChapterProviderTest` pins only as far as the restamped values. Two more that the 2026-09-11 audit found are now pinned. The group-aware read and bookmark filter is one conformance test over four probes: `MergedChapterFilterConformanceTest` runs both details lists and both readers' eligibility pass, and the three readers share one kernel (`readerChapterFilters` with `isForwardEligible`). The recents Added lane's fallback is one kernel both adapters call, `addedTarget` in `RecentsTarget.kt`, pinned by `RecentsTargetTest`.

**The gallery gap that stood here is closed.** `expandToGroup` and the reader's `groupCopyIds` both matched on the chapter number and so could spread a read mark between unrelated galleries, every gallery source numbering its first work 1. Both now read the stored stitch, which places a gallery source's chapters individually, so nothing pairs them. The duplicate-read pass on chapter completion moved with them, in the manga reader and in both novel readers.

**A ranking change restitches (2026-09-11).** The stitch ranks the sources, and that ranking picks the trunk, the copy each merged chapter shows, and which source's unnumbered chapters survive. Staleness only compared chapter rows, so dragging a source to the top in Manage sources, resetting the order, or reordering the preferred-source list left every surface on the old trunk until an unrelated chapter changed, which for a finished series is never. Each group's stitch now records the ranking it was built under (`merged_group_ranking`, a stamp from `rankingStamps` listing the members by priority), and the reconciliation compares it with the current one, so any path that changes the ranking, a backup restoring the preference included, is caught the way a chapter change is. The one-group check a screen runs before rendering also stopped scanning every merged chapter in the library: the staleness queries became views, and a filter on one group reaches into both halves. **Two stitch bugs went with it.** A title a source repeats per volume ("Afterword") paired every sibling copy with the first one in the list, pulling the sibling's walk back to volume 1, so a read mark spread to the wrong volume and a sibling-only chapter landed after volume 1; the walk now takes the copy ahead of it. And a gallery chapter registered its number as an identity, so a regular source ranked after a gallery source lost its chapter 1 as a "copy" of the gallery's work.

**Cross-source ordering measured and rebuilt (2026-09-08).** A grouped entry's chapter list was ordered by `chapterNumber`, which is not comparable across sources: one site counts a prologue, another reports its own page position. Measured on a two-source novel group, all 532 rows of one source satisfied `chapter_number = source_order + 1`, because its plugin assigns the site's `n_sort`; the other source's titles carry `siteIndex - realNumber` and the recognizer took the leading token. The two ran three apart and the list alternated between sources. **The order is now built while the sources are stitched**, in `MergedChapterOrder`, one kernel both `ChapterAggregation` and `NovelChapterAggregation` call: a chapter an earlier source placed moves a cursor to it, a new one lands just after. Komikku's `dedupeByPriority` shape (`GetMergedChaptersByMangaId`), taken over Reikai's title-first `matchKey` rather than their raw number equality. The stitched position is stamped onto `sourceOrder`, and both `restampReadingOrder` helpers stop re-sorting by number.

**The stored number is deliberately left as the source reported it.** It is not a chapter number by contract: LNReader's plugin type documents it not at all (`src/types/plugin.ts`, an empty description cell in `docs/docs.md`), its own worked example assigns `i + 1`, index-derived assignments dominate the 86 of 146 plugins that set it, and neither LNReader (orders by `position`, offering no number option at all) nor tsundoku (defaults to source order) orders by it. Nobody reconciles two sources' numbering: Komikku and TachiyomiSY have no offset handling and no per-source adjustment column, and their escape hatch is picking one source wholesale. **The reader's duplicate-read marking moved onto `matchKey` too**, since comparing numbers marked a chapter several along on the sibling source; that setting is on by default here, so some read flags predating this are wrong and cannot be told apart from a real read. **One consequence.** Sorting "By source" now really follows the source's own listing, where it had quietly sorted by number even for a single source.

**A wordless title is matched by position, not by number.** A chapter named only with numbers (`Chapter 524 - 518` against `Chapter 518`) has no title text to key on, so both copies used to survive. `MergedChapterOrder` now defers such a chapter instead of placing it: when the next identifiable one closes the run, a sibling that offered exactly as many unidentifiable chapters as the order already holds between the same two anchors is showing the same chapters, and its copies go. Any other count keeps them all, and a run with nothing after it is kept, since neither can be aligned. Measured on the two-source group: between `Ever His Humble Servant` and `Games` both sources have two untitled chapters, and the pairing is exact. This is the reconciliation the earlier note said nothing upstream attempts, and that is still true of the numbers; what makes it work here is the stitched order, which supplies positions where the numbers supply nothing. Manga is unaffected: a sibling's unrecognized chapters are dropped there rather than keyed, so no duplicate arises to fix.

**Device-verified (2026-09-09), with three cases that cannot be reached through the UI.** Confirmed on
the emulator against the restored library: the chip-view delete removes the sibling's real file; the
library Remove dialog with `All grouped sources` clears every source and leaves the entry in the
library; the merged unread count matches the stitched unit count on two entries (408 of 1485 raw rows,
44 of 248); the Unread chapter filter answers for the group (34 shown on a source whose own copies are
all unread); bookmark and mark-read propagate to every source and clear from any of them; `Download
next` queues one copy per unit and a second run skips what is on disk; the download badge counts units;
download-ahead fires, crosses a merge boundary onto the next chapter's own source, and still draws from
the group's list in downloaded-only mode where the reader's own list is exhausted; skip-read steps over
a chapter only a sibling has read; History resume and the library Resume open the same chapter; and the
unified widget draws one cover per group where three of its four series have several members among the
most recent updates. **Three cases have no UI path to the state they test**, so they rest on their unit
tests: the download badge counting a chapter two sources both hold (the app refuses to download a unit
the group already has), the bookmark read-back from a single source (bookmark writes propagate to all
of them), and an update run's notification and queue behaviour (it needs a source to actually publish).

## Decisions & tradeoffs

- **Read means read on any source.** The alternative, matching the aggregated list exactly, reproduces a quirk rather than fixing it: reading a chapter on a source that loses the dedup leaves it displayed as unread. One definition everywhere is the point.
- **Both tiers ship together.** The display layer (All list, badge, filter, sort) and the next-unread family (continue-reading, download-next, reader next-chapter, history resume) are one piece of work. Shipping only the display layer leaves five surfaces reasoning off the old definition and makes the inconsistency more visible, not less.
- **Bookmark and downloaded state ride along.** All three flags are lost by the same discard, in the same function. Doing them separately means three passes over the same code, and the downloaded case is already inconsistent with the library badge today.
- **Merged flags live on the UI item, not on `Chapter`.** Keeps DB truth intact for tracker sync, delete-after-read and backup.
- **Cache the dedup mapping, not the count.** A cached count is stale the moment the user finishes a chapter, which is exactly when they look at the badge. The mapping is invariant under reads.
- **No pure-SQL count without the cache.** It is expressible for manga, whose key is a number, but not for novels, whose key is a Kotlin-normalized title. Taking it would re-split the two content types, the failure the content-layer program exists to prevent.
- **Recomputing the aggregation per library emission is rejected.** It loads full chapter rows per member, so a library with a few dozen merged groups adds on the order of a hundred queries per emission, and emissions fire on every preference change and DB invalidation.
- **`markDuplicateReadChapterAsRead` stays.** Two independent options, both on by default here (Mihon ships them off), writing real DB state that display-time inference cannot replace: tracker counts, delete-after-read, per-source history, backup fidelity. It becomes partially redundant for display only.
- **Deliberately out of scope:** per-chapter page progress (a merged chapter shows the winning row's progress, and any-member-read does not solve partial progress), and whether the Updates feed should hide a sibling's copy of an already-read chapter. Both are real, both are separable.
