# Content layer: the sources surface (compiled-APK novel extensions)

> **Standing rules for every session working this surface.** The program rules bind here and are not restated: they live in [.claude/rules/content-layer.md](../../../.claude/rules/content-layer.md), which loads every session (write-once, typed capability slots over nullable soup, derive-or-do-not-own, the pin-once ladder, decline expiry). For the program context read [content-layer-architecture.md](content-layer-architecture.md) first, and [content-layer-browse-surface.md](content-layer-browse-surface.md) for the catalogue this plugs into. Six more bind on this surface specifically. **One seam:** every novel feature reaches a source only through `NovelSource`, and the differences between the three source kinds (LN plugin, tachiyomi-format APK, IReader APK) are typed capabilities, never a kind check in shared code. **One extension pipeline:** store, install, trust, signature and library-version checks exist once, in Mihon's pipeline, as `// RK` islands, and there is never a parallel loader. **Identity is the prefixed text id** (`tachiyomi:<id>`, `ireader:<id>`), never the raw number, because the two ecosystems share 26 ids and seven more collide with keiyoushi manga sources. **IReader's API stays pinned at 1.5.1** until an upgrade has been checked for the duplicate `eu.kanade.tachiyomi` classes its `main` branch has since added. **Extension output is untrusted input** ([security.md](../../../.claude/rules/security.md)): APK chapter text is sanitized like LN plugin output. **A service with a Reikai tracker belongs to the tracker:** where a source's own tracking hooks and a Reikai tracker write the same site, the tracker is the only writer and the source's tracking settings are hidden, never left inert.

## Goal

Load the two compiled-APK novel ecosystems beside the LNReader plugins: tsundoku's novel extensions (the NovelSourcery repository) and IReader's extensions. A novel from either reads, downloads, updates, migrates and tracks exactly like one from an LN plugin, and the Extensions tab installs, trusts, updates and removes them like manga extensions.

## Why

Requested in `unseensnick/Reikai#31`. Reikai's novels come only from LNReader plugins run in QuickJS, and both repositories ship compiled APKs instead, which the app ignores: the manga loader only accepts the `tachiyomi.extension` feature. Measured against the plugin repository Reikai points users at, tsundoku's repository adds 42 sites the plugins do not cover (Wattpad and the five Syosetu sites among them), and IReader's adds at most 75, fewer after renamed and dead entries.

Grounding also found a defect that exists today: adding the NovelSourcery repository under the manga extension stores follows its `index_v2` pointer and lists all 149 novel APKs as installable manga extensions, none of which ever loads.

## Approach

### Three source kinds, one seam

`NovelSource` is already the only door: browse, global search, details and chapter refresh, the reader's text load, downloads, the library update job and migration all reach a source through it and `NovelSourceManager`. `NovelSourceManager.get` and `getAll` run `ensureLoaded` before answering, so widening `ensureLoaded` to register the APK sources reaches every consumer that looks a source up there. Several consumers also warm the LN installer directly first; those calls stay and become redundant rather than wrong. Each APK kind gets one adapter implementing `NovelSource`; nothing above the seam learns there is more than one kind.

What the plugins express as raw LNReader JSON becomes a typed capability, because an APK source speaks a different contract:

- **Filters** are either the LN schema or a Mihon `FilterList`. The catalogue's filter slot renders `NovelSourceFilterSheet` for the first and Mihon's `SourceFilterDialog` for the second, which already takes only a `FilterList` and callbacks. IReader's filter model is translated both ways, since it is not Mihon's: its `Check` stands for both a checkbox and a tri-state (a nullable value plus an `allowsExclusion` flag), its `Group` holds mixed filters where Mihon's holds one type, it has no `Separator`, and the search query travels as a `Title` filter rather than an argument. With that translation one sheet serves all three kinds. Where filters apply is part of the type (`NovelFilters.applyToSearch`): an LNReader plugin's search takes no options, so its filters narrow the listings and applying them drops the query, while a Mihon filter list travels with the search and keeps it, as manga's does. This changes the browse record's premise: the filter split was "manga means `FilterList`, novels mean JSON", and it is now per source kind (amendment below).
- **Search takes filters.** `NovelSource.search` and `browse` both take the filter state, and each format ignores it where its own contract has no place for it.
- **Settings** are the LN schema (`NovelSourceSettingsSheet`), an Android preference screen (`ConfigurableSource`, hosted by `SourcePreferencesScreen` through a resolver keyed on the novel source's text id, since the number alone can name a manga source or two novel sources), or absent (IReader sources have no settings screen; their per-book commands are below).
- **Images:** covers and in-chapter images use the source's own client and headers, as `MangaCoverFetcher` does for manga, through `NovelImageRequests` (A4c). An app's source icon is its package icon, named by a `reikai-extension-icon://<package>` address that a Coil fetcher answers, since every novel icon slot takes an address.
- **Listings:** an IReader source declares one or two listings, mapped onto Popular and Latest; `supportsLatest` is true only when a distinct Latest listing exists.

### One extension pipeline, three kinds

Mihon's `ExtensionLoader` learns the other two manifest features rather than gaining a sibling. Each kind has its own feature, meta-data keys and library gate:

| Kind | Feature | Class key | Library gate |
|---|---|---|---|
| Manga | `tachiyomi.extension` | `tachiyomi.extension.class` | 1.4 or 1.6 |
| tachiyomi-format novel | `tachiyomi.novelextension` | `tachiyomi.novelextension.class` (also `.nsfw`, `.novel`) | 1.4 or 1.6 |
| IReader | `ireader` | `source.class` (also `source.nsfw`) | major 2 only |

The `tachiyomix.*` keys are shared by the first two. An installed extension carries its kind, read from its feature, and `ExtensionManager` files novel kinds into maps of their own as they arrive, so its manga maps stay manga only and nothing that reads them (source registration and stub rows, the lists, the update count, backups) ever sees a novel APK. The install receiver and private installs accept every kind through the same feature check. Trust is unchanged: a store's signing key covers its extensions, and a keyless store's extensions are trusted per version by the user. IReader's index carries no key, so its own repo's store carries the key its APKs are signed with, read off them; any other IReader-format store is keyless.

Class loading is unchanged too (`DelegateLastClassLoaderCompat` over the app's loader), and for tsundoku-format APKs that is sufficient. They are keiyoushi-derived: their host-provided library bundle is identical to keiyoushi's manga bundle, and they compile against `tsundoku-otaku/extensions-lib` v1.6.0-3, which is tachiyomix 1.6 plus two interfaces. Every member name in that library exists in Reikai's source-api and `core/common` except `SourceTracker` and `RateLimited`, which the whole `madaranovel` theme, NovelUpdates and the `syosetu` theme implement, so those classes fail to load until source-api has them.

### Identity

Novels store their source as text. An APK source's number becomes `tachiyomi:<id>` or `ireader:<id>`, taken from the running source object (IReader's generated `Extension` class overrides `id` with the build-assigned value, which matches its index). Bare numbers are ruled out: 26 sources carry the identical id in both repositories (FreeWebNovel, NovelFull, Ranobes, LNMTL and more). None of the 279 LN plugin ids is numeric or contains a colon, and nothing parses a novel source id as a number. Download folders are named after the id through `DiskUtil.buildValidFilename`, which turns the colon into `_`.

Seven sources also share a number with a keiyoushi manga source (DragonTea, NovelCool and WoopRead from tsundoku; MangaTX, Toonily, ZinManga and Wuxiaworld from IReader). A tsundoku-format extension names its own settings file `source_<number>`, so a manga and a novel extension for the same site would share one file; no host choice can prevent that. IReader's preference bridge keys on the package name instead, as IReader itself does, so its 26 twins never share settings with their tsundoku copies.

### The tachiyomi-format adapter

Each APK ships its own `eu.kanade.tachiyomi.source.NovelSource` interface, a standalone copy the host cannot reference by type. The host therefore declares `fetchPageText(page)` on source-api's `Source` with a throwing default, and the extension's own method answers the interface call across class loaders. tsundoku's host and IReader's compatibility layer both load these APKs this way.

The adapter wraps a `CatalogueSource`: popular and search go to `getPopularManga` and `getSearchManga`, details and chapters to `getMangaUpdate`, and chapter text to `getPageList` then `fetchPageText` for each page, joined. Page URLs are the extension's to decide (a Madara theme returns an absolute URL, NovelUpdates the chapter URL), so the adapter always asks `getPageList` and never builds a page itself, as tsundoku's host does. The contract lists chapters newest first and novels keep them in reading order, so the list is reversed; a source's own "reverse chapter list" setting therefore reverses reading order too, as it would a manga source's. Status crosses through `NovelStatusCode`'s one table, read both ways; the upload date becomes an ISO instant, the one form the novel date parser keeps to the millisecond. A listing page carries the source's `hasNextPage` (`NovelItemsPage`), since a tachiyomi source may answer a page past the end with an error rather than nothing.

### The IReader runtime

IReader extensions expect the host to provide IReader's own API, published as `io.github.ireaderorg:source-api-android:1.5.1`, with Ktor 3.3.2, IReader's Ksoup build (package `com.fleeksoft.ksoup`), kotlinx-datetime and Kermit. Reikai depends on that artifact pinned. Version 1.5.1 contains only `ireader.*` (`ireader.core` and `ireader.common`); IReader's `main` has since added copies of `eu.kanade.tachiyomi.*` classes that would clash with Reikai's own source-api at build time. Koin arrives transitively but only `CloudflareBypassModule` uses it, so it is excluded, which keeps the architecture rule against Koin intact. The date library is binary-safe: 0.7.1's `Instant` and `Clock` are type aliases for the `kotlin.time` types, and 0.8.0 removed nothing else.

Every source is constructed from `Dependencies(HttpClientsInterface, PreferenceStore)`, and both are interfaces, so Reikai implements them itself. The HTTP clients are Ktor over Reikai's own OkHttp client through Ktor's OkHttp engine, so IReader sources inherit FlareSolverr, the WebView fetch, the shared cookie jar, the cache and the app's user agent. Of the plugins IReader's own client installs, only Gson content negotiation is kept, since an extension's plain classes decode through it; its cookie and cache plugins would duplicate what the OkHttp client already does. Ktor names itself `ktor-client` when a request names no agent, and the app's interceptor leaves a named one alone, so the app's agent is set per request on the Ktor side, never over an extension's own. `BrowserEngine`, `SSLConfiguration` and `CookieSynchronizer` are final classes the artifact supplies, so the browser is IReader's own WebView engine, which PandaNovel, Novebo and Mtlarchive call; it and the cookie pieces are built on first use, because IReader's cookie jar loads the WebView provider in its constructor. The preference store bridges to `PreferenceStore` under `ireader_storage::<package>::`, as LN plugins keep theirs under their id.

A chapter arrives as a list of pages, a `Text` per paragraph and `ImageUrl` for pictures; the adapter assembles them into the chapter HTML the readers take. Details and chapters are requested with no commands, as IReader requests them. A source that declares the WebView fetch commands (`Detail.Fetch`, `Chapter.Fetch`, `Content.Fetch`) answers `NovelSource.pageFetch`, which hands it a page the user loaded in the in-app browser, the way through for a site that blocks its requests: the novel's WebView offers its details and chapters, the reader's WebView the open chapter's text, which is saved as that chapter's download.

R8 has to keep what extensions call by name: `ireader`, `io.ktor`, `kotlinx.io`, `com.fleeksoft.ksoup` and Kermit join the existing keep list, which already held `kotlinx.datetime`. The artifact ships no consumer rules of its own. With Koin excluded, R8 is told not to warn about it, since IReader's `CloudflareBypassModule` is the one class that refers to it. The newer versions Reikai already uses win resolution (kotlinx-datetime 0.8.0, coroutines 1.11, serialization 1.11, OkHttp 5.5); IReader's own app runs these extensions on the same datetime and a newer Ktor, and its API never names the `kotlinx.datetime.Instant` that 0.8.0 dropped. Measured on the minified nightly, the arm64 APK grows from 33.30 MB to 34.47 MB, 1.17 MB, well under the 3 to 5 MB estimated from the artifacts' compressed size; there is no ceiling (owner).

### Source-side tracking

`SourceTracker` is a tsundoku-library interface through which an extension syncs reading to its own site. One kernel, `SourceTrackerKernel`, runs it for both content types, because a manga extension can implement it too. It holds chapter events per entry for three seconds, merging events of the same kind and starting over on the other kind, re-reads the entry when it fires, passes category names without the uncategorized one, and reports each call's failure with a toast without stopping the next. An unread that leaves a lower chapter read tells the site that chapter was read, as tsundoku does. The interface is found with a plain type check rather than reflectively: a novel app bundles none of source-api, so its classes implement the host's own interface. Reads reach it from `SetReadStatus`, `SetNovelReadStatus` (which the novel reader uses), the manga reader and the notification's mark-read action, each inheriting incognito from where it sits. Favorites reach it from `awaitUpdateFavorite` on both types and from the paths that write the favorite directly: the manga browse add, the library remove and the novel merge host. An add or remove waits the same three seconds and goes only if the library still agrees, so an add inside a transaction that rolls back sends nothing, categories filed just after an add go with it, and an add and a remove inside the wait cancel out. Taking a series out of a merge group and undoing that notify for both types (owner). Migration notifies behind a preference, tsundoku's `source_tracker_run_on_migration`, on by default: the target as favorited, its read chapters when read state was carried, and the old entry as unfavorited on a replace. Restore never does.

`RateLimited` is honoured for novels only, under the standing ruling that download pacing is novel-only. It reaches the novel side as `NovelSource.minimumRequestDelayMs`, which lifts a source's download delay, its own or the global one, to at least that value, and hides the Pacing screen's shorter options for it. As in tsundoku, which enforces only the minimum, a source without its own delay follows the global one (owner): `recommendedDelayMillis` and `recommendedPermits` are not used, since Reikai's pacing has no burst to seed.

### NovelUpdates: the tracker owns the site

Reikai's NovelUpdates tracker and the NovelUpdates extension write the same site, and the extension's version is destructive: its notes sync reads the note and tags with a regex that stops at the first quote and writes the truncated text back, and on a failed read writes its progress line over the whole note. The tracker already decodes the note as JSON and refuses to write what it cannot parse. So the tracker does all NovelUpdates tracking:

- It gains **release marking**. The site keeps one bookmark per series, not a tick per release: `readinglist_update.php` with `checked=yes` moves it to that release, earlier or later, and `checked=no` only takes it off. A chapter from the site's own app or plugin links to the release it is (`/extnu/<rid>/`), so a read bookmarks the release it links to; for a novel from another source the release list comes from `nd_getchapters` keyed by the post id, and a release is bookmarked only on an exact single number match (`ChapterRecognition.parseChapterNumber`), never for an unnumbered chapter. Otherwise the bookmark is left and the note still updates (owner, 2026-09-22).
- It gains **unread push**, opt-in: marking chapters unread moves the bookmark back to the release of the highest chapter still read and lowers the note to match, never above what the site had, and not at all for an unread above the site's progress. It runs off `SetNovelReadStatus` through `PushNovelUnread`, once per unread however many merged sources it spans.
- It gains **never move backwards**, on by default, for automatic read pushes only: rereading an earlier chapter leaves the site, and the app's copy of its progress, where they are. A progress set by hand in the tracking sheet always goes through (owner, 2026-09-22).
- It **auto-binds** novels added from the site's own app or LNReader plugin, matched by host through `OwnedSites`, so those novels track with no setup.
- `OwnedSites` gives the tracker the site, logged in or not (owner, 2026-09-22): the dispatcher never calls the extension's tracking hooks, and `SourcePreferencesScreen` removes the six settings the tracker took over (`pref_enable_tracking`, `pref_track_last_read`, `pref_track_notes`, `pref_track_unread`, `pref_protect_highest`, `pref_reset_cache_toggle`) after the extension builds its screen. "Return full HTML" stays, as a reading option.

The other extensions with tracking hooks (the Madara novel theme, Konkon, KuuPress, WtrLab) keep going through the dispatcher; Reikai has no tracker for their sites.

### Auto-binding trackers, for both content types

Manga auto-binds trackers today through `EnhancedTracker`; novels bind nothing automatically. The interface bundles two ideas: "this tracker is the source's own server" (a settings row only when that source is installed, a no-credentials login, no refresh button, hidden for other sources, excluded from Fill from tracker) and "this tracker auto-binds entries from its sources" (bind on add, progress sync on bind, match in the tracking dialog, re-point on migration). NovelUpdates wants only the second; implementing the interface would hide it from every other source and turn its real login into a no-op.

So `EnhancedTracker` stays untouched and a Reikai-owned capability, `AutoBindTracker`, carries the second idea, written once:

- **Bind on add.** One routine, `bindOnAdd`, takes every logged-in tracker whose capability accepts the entry's source, tries a match, binds through the type's own write, and carries on past a failing tracker. `AddTracks.bindEnhancedTrackers` keeps its name and hands its body to it (`// RK`), manga still running `SyncChapterProgressWithTrack` after a bind; the three manga server trackers reach it through `EnhancedAutoBind`, so their files stay untouched. `BindNovelTrackers` is the novel twin, binding through `AddNovelTrack`. Both run off the add through `AutoBindOnAdd`, started once the favorite write lands and never before, so a dismissed category picker binds nothing and the categories are filed without waiting on the network.
- **Match in the dialog.** `offerTrackers` decides, for both types, which rows the tracking dialog offers and whether a tap matches or searches. A tap matches only for an entry the tracker accepts. `offeredOnlyWhenAccepted` keeps the server-tracker behaviour of hiding a row for other sources, and NovelUpdates answers it false, so its row and any binding it already has stay for a novel from any source.
- NovelUpdates implements the capability. Its bind reads the user's lists first: a series already on one keeps its list and note, so binding (by hand or on add) never lowers the site's progress.

Declined while their premise holds (owner, 2026-09-22): a novel copy of the pull-style progress sync, a shared migration re-point kernel, and the server-tracker screen behaviours as shared capabilities. No novel tracker pulls progress down, re-points on migration or is a source's own server: NovelUpdates binds to the series on its site, so novel migration's plain copy is correct. Revisit if one ever does.

## Sequenced steps

Each step is verified on the emulator before the next starts; the checks named are the ones that would catch it being wrong.

- **A1, the contract.** `fetchPageText` on `Source`, `SourceTracker` and `RateLimited` in source-api. Verify: compile, and the existing manga extensions still load.
- **A2, the tachiyomi-format kind in the pipeline.** Feature, keys and gate in `ExtensionLoader`, the kind on `Extension.Installed`, novel kinds kept in their own maps in `ExtensionManager`. Verify: install a NovelSourcery APK; it loads as a novel extension and no manga source or stub appears.
- **A3a, stores know what they serve.** Each index entry's field 8000 (or, in an old-format index, the novel package namespace) sets the kind of an available extension, and `ExtensionManager` keeps novel entries out of the manga list. Verify: with the NovelSourcery repository added, none of its 149 extensions reach the manga list.
- **A3b, the Repos screen.** A Reikai-owned screen shaped like the download queue: one card per repository with its format, content type and extension count, a details sheet, one Add that detects whether the address is an extension store or an LN plugin repository, and a refresh covering both. Mihon's five screen files are deleted and manifested. Verify: add, refresh and remove each kind on device, and the deep link.
- **A4, typed novel capabilities.** Filters, settings, search filters, image client and headers, listings. Verify: LN plugins behave exactly as before, device-walked on browse, search, filters, settings and covers.
- **A5, the Extensions tab.** Novel APK rows using the manga APK actions and Not-loaded reasons, with install, update, trust, the update check and notice, and a details screen without the source list. Registration moved to B1, since nothing can be registered before its adapter exists.
- **B1, the tachiyomi-format adapter and its registration.** The adapter, registered through `ensureLoaded`; the LN plugin manager's installed list filtered to LN plugins, since it reads every registered source as a plugin; the details screen's source list and switches on the novel disabled list; novel APKs in extension backups; the Sources sheet hiding incognito for an APK source whose APK is not loaded, where the key resolves to nothing. Verify: one conformance test over this adapter and `LnPluginSource` against the `NovelSource` contract, then a Madara-theme novel browsed, added, read, downloaded and updated on device.
- **B2, the tracking dispatcher**, both content types. Verify: unit tests over a fake tracking source, each mutation-checked.
- **B3, rate-limit hints** into novel pacing.
- **B4, auto-binding trackers** for both content types: the capability, the bind-on-add routine, and the dialog. Verify: manga auto-binding unchanged on device against a manga enhanced tracker, and one conformance test over the routine for both types.
- **B5, NovelUpdates tracker ownership.** Release marking, unread push, never-backwards, auto-bind acceptance, the hidden extension settings. Verify: tests, then one live check on the Fold with the owner's OK on the day, on a novel the owner does not care about, unmarked afterwards.
- **C1, the IReader dependency.** Pinned artifact without Koin, R8 keeps. Verify: a minified nightly builds and launches; the APK size is measured and recorded here.
- **C2, IReader host services.** HTTP clients, preference bridge, browser engine.
- **C3, the IReader kind and adapter.** Loader construction through `(Dependencies)`, the IReader index parser (a flat array with no `repo.json` and icons named after the APK), listings, filter translation, pages to HTML. Verify: FreeWebNovel browsed, added and read on device, beside its tsundoku twin.
- **C4, page fetch.** The WebView fetch commands as a typed capability and the in-app browser's page actions.
- **D, the behaviour inventory and records.** Walk what tsundoku's and IReader's hosts do with these APKs and mark each behaviour present, deliberately dropped or missing; update the FAQ in `docs/about.md`, `feature-ports.md` and the CHANGELOG; remove the roadmap line.

## Key files

- `source-api/.../source/Source.kt` (`fetchPageText`), and the new `SourceTracker.kt` and `RateLimited.kt` beside it.
- `app/.../extension/util/ExtensionLoader.kt` (`isPackageAnExtension`, `loadExtension`), `domain/.../extension/model/Extension.kt`, `app/.../source/AndroidSourceManager.kt`, `app/.../extension/util/ExtensionInstallReceiver.kt`.
- `data/.../extension/service/ExtensionStoreService.kt` (`fetch`), `data/.../extension/model/NetworkExtensionStore.kt`, `app/.../domain/extension/interactor/GetExtensionsByType.kt`.
- `reikai/novel/source/NovelSource.kt` and `NovelSourceManager.kt` (`ensureLoaded`); the adapters beside them.
- `reikai/presentation/browse/catalogue/EntryCatalogueScreen.kt` (the filter and settings slots), `app/.../ui/browse/source/browse/SourceFilterDialog.kt`, `app/.../ui/browse/extension/details/SourcePreferencesScreen.kt` (`populateScreen`).
- `reikai/presentation/browse/extension/ReikaiExtensionsTab.kt`, `ExtensionsProvider.kt`.
- `reikai/domain/novel/interactor/SetNovelReadStatus.kt`, `UpdateNovel.kt` (`awaitUpdateFavorite`), `app/.../domain/chapter/interactor/SetReadStatus.kt`, `reikai/novel/download/NovelDownloadPacing.kt` (`floorFor`).
- `app/.../domain/track/interactor/AddTracks.kt` (`bindEnhancedTrackers`), `SyncChapterProgressWithTrack.kt`, `reikai/domain/track/autobind/AutoBind.kt` (`offerTrackers`, `bindOnAdd`), `reikai/presentation/track/EntryTrackInfoDialog.kt` (`registerAutoBind`), `mihon/domain/migration/usecases/MigrateMangaUseCase.kt`, `reikai/domain/novel/interactor/MigrateNovelUseCase.kt`.
- `app/.../data/track/novelupdates/` (`NovelUpdates.push`, `NovelUpdatesApi`).
- `app/proguard-rules.pro`, `gradle/libs.versions.toml`.

## Status

Complete, shipping in 0.4.0; D's behaviour inventory is below. A4a (filters, search and listings as typed capabilities) has landed: `NovelSource.browse` and `search` take a typed filter state, the LNReader option building moved into its adapter, and the catalogue, saved searches and the feed follow the filter type. A4b (settings) has landed: `NovelSource.settings` is either an LNReader schema, read and written through the host's per-plugin storage, or a `ConfigurableSource` preference screen, which `SourcePreferencesScreen` hosts for a novel source by its text id; the catalogue and details open whichever one the source has. A4c (images) has landed: `NovelImageRequests` answers each source's image client and headers without running a plugin, an LNReader source from the site and `imageRequestInit` headers its last load saved in the seen-sources record, an APK source from its loaded extension, and covers, the text renderer, downloads and the WebView reader all fetch through it; the WebView reader routes a chapter's online pictures through `appassets.androidplatform.net` and serves them from the text renderer's fetch and cache, answering only addresses its own rewrite produced. A chapter picture gets the source's client and headers only on the source's own host or a subdomain of it; any other host gets the app's client with the source's user agent and its site as Referer, since whoever wrote the chapter names its pictures and a source's headers can carry a login (owner ruling R4, 2026-09-23). B1 (the adapter and its registration) has landed: `TachiyomiNovelSource` wraps each catalogue of a loaded novel app, `NovelSourceManager` follows the loaded apps and registers, replaces and drops them beside the plugins, and remembers them in the seen-sources record; the LN plugin list keeps only plugins, the details screen lists an app's sources with switches on the novel disabled list and a settings gear, novel apps join extension backups, and the Sources tab names each novel source's format when both are listed. BoxNovel browsed, searched, filtered, added, read from Chapter 1, downloaded and updated on device. A5 (the Extensions tab) has landed: novel APKs list under Novels through the same rows, actions and partition as manga ones, one provider serving both kinds so a refresh fetches each store once; update, trust, the update check and the launch notice reach the novel maps; the badges count both kinds of novel extension; the details screen opens with its header, uninstall and incognito, and a novel APK's sources go incognito with their whole APK, as a manga extension's do. C4 (page fetch) has landed: an IReader source declaring the fetch commands answers `NovelSource.pageFetch`, and the in-app browser offers its page for the novel's details and chapters from the novel screen, and for the open chapter's text from the reader, which reloads the chapter once it is saved. Chapter text goes through `NovelChapterSaver`, which the download manager uses too. On the emulator, Wuxiabox's details updated from its page and chapter 4068 was saved from its page, shown as downloaded and reloaded in the reader; LightNovelHeaven's novel page, which a request reads with no chapters, gave 357 once loaded in the browser. Wuxiabox reads no chapters from any page, since its theme declares `Chapter.Fetch` with no chapter selectors, as it does in IReader. C3c (the IReader store listing) has landed: IReader's index is recognised by its entries (a source id and a boolean nsfw, which no tachiyomi index entry has) and kept as a keyless legacy row at its own `index.min.json` address, which Mihon never stores, since it rewrites an old-style address to `repo.json` and an old backup restores a bare base address. Its listing keeps the newest copy of each package, since the live index lists five packages twice with the stale copy last, and names icons after the APK. The store is named after its GitHub owner by the rule plugin repos use, a keyless store's details sheet shows no signing key, and an available row whose index names no site shows no WebView button, which also covers Mihon's old-style stub rows. On the emulator the store listed 138 extensions under `IReaderorg`, survived a refresh, NovelFull installed as 2.9 rather than the stale 2.1, came up Untrusted and loaded once trusted (IReader's own repo is trusted by its key since, see Decisions), and the installed FreeWebNovel, Golden and LightNovelHeaven showed no update. Half the index's icons do not exist in its repository and draw the placeholder. An installed extension flagged Orphaned keeps the flag until a restart when a store listing it is added, as upstream does for manga. C3a (the IReader kind in the loader) has landed: an apk declaring the `ireader` feature loads as its own kind, library 2 only, named and flagged from its `source.*` metadata, built through `(Dependencies)` and carried in the extension's source list by a holder; its catalogue is `ireader:<id>`, and the extension details, source switches, incognito, the Extensions rows and their format label follow the kind rather than assuming a tachiyomi-format app. FreeWebNovel 2.17, sideloaded on the emulator, showed as Untrusted, loaded once trusted, and switched off and on under its `ireader:` id, which matches its index id. C3b (the IReader adapter) has landed: `IReaderNovelSource` browses its first two listings as Popular and Latest, searches through the Title filter, carries IReader filters into the sheet and writes the picked values back, keeps the chapter order the source gives, and renders text pages as paragraphs and linked, embedded or fetched pages as pictures; an IReader source fetches its images with the headers its cover request carries. FreeWebNovel browsed, sorted, searched, opened with its 495 chapters oldest first, read and added on the emulator. LightNovelHeaven (Madara) loaded its covers with the Referer its cover request sets, and a Golden chapter of picture pages showed all eleven pictures. C2 (IReader host services) has landed: `IReaderHostServices` builds each extension's `Dependencies` from the shared clients and a per-package preference store, pinned by unit tests on the user agent, the Gson decoding and the key namespace; the browser engine is only exercised by a real extension, so C3's device check covers it. C1 (the IReader dependency) has landed: the pinned artifact without Koin and the R8 keeps, with a minified nightly that builds, launches, and keeps `ireader.core.source.HttpSource` and its public methods by name in the dex. B5 (NovelUpdates owns its site) has landed in code: release ticking, never-backwards, opt-in unread push, auto-binding and the ownership table, with the extension's six tracking settings hidden on device. **Its live check passed on 2026-09-23**, on the emulator signed in to the owner's account, once `nd_getchapters` served real release ids again (c84 was `21772310`). Adding a Novel Updates novel bound it on add; reading chapters 1 and 2 moved the reading-list bookmark `c1 -> c1`, `c1 -> c2`; with Move back on unread on, unreading chapter 2 moved it back to c1 and lowered the note to 1. It found the unread push built on ticks, which the site does not keep (see Decisions), and fixed it the same day. One miss is unexplained: the note write of the first read after a fresh install never landed while that push's bookmark did, and the same write landed on the next two reads. Some requests to a WebView-served host never appear in OkHttp's log, so the log alone cannot show what reached the site; the site's own Reading List Logs can. The owner cleaned up afterwards and kept the Novel Updates extension on the emulator. B4 (auto-binding) has landed: one capability and one bind-on-add routine for both types, with Komga and Suwayomi binding on add on device, a dismissed picker binding nothing, and the tracking dialog asking the capability; no novel tracker implements it until B5. B3 (pacing hints) has landed: a novel app's declared least delay floors its download pacing and the delays the Pacing screen offers it. B2 (source-side tracking) has landed: one kernel tells an extension that syncs to its own site about reads, unreads, adds, removes and, behind a setting, migrations, for both content types, with a toast when the site call fails. A1 (the contract) has landed: `fetchPageText`, `SourceTracker` and `RateLimited` in source-api, pinned by `SourceApiContractTest`, with installed manga extensions (library 1.4 and 1.6) loading and browsing as before. A2 (the novel kind in the pipeline) has landed: BoxNovel, a Madara-theme APK implementing `SourceTracker`, loads as a novel extension once its store key trusts it, and before that sits as untrusted in the novel maps; neither state reaches a manga list, source or stub row. A3a (stores know what they serve) has landed: the NovelSourcery repository's 149 extensions no longer list as manga. A3b (the Repos screen) has landed, reading the listings `ExtensionManager` and `LnRepoRegistries` already keep, so opening it downloads nothing when the Extensions tab has fetched. Nothing registers the novel maps as sources until B1. Scouted and grounded 2026-09-21 against `refs/tsundoku` `b04f9a4d3`, `refs/IReader` `de8cf8b31` (API read at `548bd934f`, the 1.5.1 release), `NovelSourcery/extensions-source` `main`, `tsundoku-otaku/extensions-lib` `v1.6.0-3` and `IReaderorg/IReader-extensions` `master`. Revert point: tag `checkpoint-apk-novel-extensions-pre` on `e495ba3df`.

## The behaviour inventory (step D)

What tsundoku's host (`b04f9a4d3`) and IReader's (`de8cf8b31`) do with these APKs was walked on both sides and each item checked off against Reikai. Present means pressed on the emulator unless marked traced, since a traced chain has passed review here before and still been broken (browse record).

**Present, pressed.** Installing from a store, trusted by its key with no prompt (Wuxia Space), and uninstalling back to Available; browse, search, filters, details, chapters and reading (B1, C3b); downloads and the library update (B1); global search across JS, APK and IReader sources together; migration from an APK source to an IReader one (3193 chapters) and back; a tsundoku-format app's settings screen, read and written; a backup carrying the apps' settings files; incognito per app (C3a); WebView and share on the app's own page addresses; icons, borrowed where an app ships none. **Present, traced:** restoring those settings, which is Mihon's restorer by file name, and an app update, which runs the manga path and had none pending.

**Present by design, not as the reference does it.** The kind comes from the manifest feature, where tsundoku also reads a `.novel` metadata flag (Decisions). The Extensions list does not filter novel apps by language, as it never has for plugins. Pacing applies to downloads only (Decisions).

**Missing, and fixed here.** NovelSourcery's ReadWN theme (FansMTL, Wuxiabox, WuxiaSpace) names a chapter only as its page's picture and then fetches the page's own empty address, so every chapter read blank; the adapter now fetches from the picture address (`85f79a645`), and tsundoku has the same defect. A chapter a source returns empty read as a blank page for every kind; it is now a failed load with a retry (`02eb3cc54`), where IReader errors on an empty page list. IReader drops a listed entry with no title, and Reikai now does too, and an app named only by its label no longer reads "Tsundoku: X" (both `85f79a645`). Madara chapter lists came from the novel's own address once IReader's theme could no longer reach them (Decisions). Pressing the rest turned up four more, fixed on both content types: a stray separator on extension rows, a version line that differed by kind (`50e864693`), two same-named sources with nothing to tell them apart in search and migration (`f9e3c1ee6`), and a crash on a result a source listed twice (`5535d1ea8`).

**Deliberately not taken.** tsundoku's host answers `getPageList` for a novel source with one page, but all 150 NovelSourcery extensions (`ae33a5c`) supply their own, so nothing relies on it. IReader's default repo (Reikai ships none, as Mihon ships none), its per-repo switch, install-everything, health check and security scan, APK folder drop-in and load retry (Reikai shows Not loaded with its reason). An IReader app's `source.icon` metadata, which is the address its store already lists. A details refresh hands an app only the novel's address; one extension reads the stored title, and only when its page has none.

## Decisions & tradeoffs

- **Both ecosystems, full version, in 0.4.0** (owner, 2026-09-21). IReader was offered as a separate item because it needs a second runtime; the owner ruled it in.
- **Ids are prefixed by format, not by app** (owner, 2026-09-21): `tachiyomi:` names what the extension is, a manifest format any repository can publish, where `tsundoku:` would mislabel a repository that is not tsundoku.
- **No APK size ceiling** (owner, 2026-09-21): measured and recorded rather than capped. Trimming the kept code to what extensions call today was declined, because an extension update calling something new would crash.
- **Source-side tracking follows what the user did** (owner, 2026-09-21): taking a series out of a merge group and undoing it notify the site for both types, a failed site call shows a toast rather than failing silently, and a replace migration tells the site the old entry was removed, behind the same setting as the rest of migration.
- **Adds and removes wait, unlike tsundoku's** (owner, 2026-09-21). tsundoku sends them at once, which tells the site about an add whose transaction then rolls back, and sends it before the add flow files categories; WtrLab picks the site folder from those categories. Every extension's handler sets a state rather than toggling, so sending only the settled state is safe. Left open: a group add that asks for categories in a picker after favoriting sends none when the user takes longer than the wait, as before.
- **Tracking is verified by tests plus one live check** (owner, 2026-09-21), on the owner's account, with explicit consent on the day.
- **A Madara chapter list the site refuses is fetched from the novel's own address** (owner, 2026-09-23). IReader's Madara theme asks `admin-ajax.php` for `manga_get_chapters`, which current Madara sites answer with 400, then the site root's `ajax/chapters/`, which does not exist; of the 6 published Madara sources a probe could test, 5 returned no chapters this way while each novel's own `ajax/chapters/` held them all. The theme is compiled into each APK and upstream was not asked, so `MadaraChapterEndpoint`, the first interceptor on IReader's HTTP client, answers a refused ask from the novel's address, found through WordPress's `?p=<post id>` redirect (read from `Location` too, since the client may hand the 301 back unfollowed), with the refused ask's body headers dropped, since a site drops a bodyless request claiming a length. An ask the site answers is left alone. LightNovelHeaven went from 0 chapters to 1236 on a normal open.
- **Rate limits pace downloads only** (owner, 2026-09-23). tsundoku throttles every request to a novel site's host (`SourceRateLimitPolicy`), with a default for sources that declare none. The three NovelSourcery extensions declaring a minimum (NovelFire, Ranobes and the Syosetu theme) already pace their own client at it, so browsing and reading are paced without a host policy; an app-wide throttle is a separate tsundoku feature and was not taken.
- **A chapter named only as a picture is fetched from that address** (owner, 2026-09-23). The ReadWN theme's `getPageList` returns `Page(0, imageUrl = chapterUrl)` and its `fetchPageText` reads `page.url`, which is empty. The adapter hands such a page over with the picture address as its own, which no other NovelSourcery extension produces, since none returns picture pages.
- **The per-book commands sheet was not built** (owner, 2026-09-23). Of IReader's 143 published entries, 136 declare only the three fetch commands and none declares `Note`, `Text`, `Toggle` or `Range`; Ranobes (en) adds one `Chapter.Select` whose value it never reads, so showing it would offer a choice that does nothing. Revisit if an extension declares one it reads.
- **IReader's own repo is trusted by its signing key** (owner, 2026-09-23). IReader itself has no trust gate: its loader runs any `ireader` APK, and its `verifySignature` checks only a package prefix. Every APK sampled from its repo (12, across seven languages and both library versions) is signed with `f4527fa6…c17c`, so the store built from `IReaderorg/IReader-extensions` carries that key and its extensions load without a prompt, like a keyed store's; a changed key falls back to asking. Declined: trust on first use (one hasty trust becomes standing) and no gate at all.
- **An installed app whose own icon shows nothing borrows one** (owner, 2026-09-23). Every IReader APK ships a one-pixel transparent icon, and IReader shows the repo icon instead. The fetcher behind every novel APK source icon treats an icon with no visible pixel as missing and tries, in order, the icon its store lists for the package, then an icon any novel store or plugin repo lists for a site the app runs on, matched by host; the listings are recorded as they arrive (`NovelIconHints`), so a cold start has them. Borrowing by name for an available row was declined: of 31 name matches against IReader's own code, 3 are different sites. A missing icon falls back to the placeholder, and novel extension rows inset their icon as the novel source rows do, since novel icons are full-bleed.
- **An unread moves the site's bookmark back rather than unticking** (owner, 2026-09-23). The live check found the site keeps one bookmark per series (`bookmarkme` in its `readinglist.js`): the extension's untick, `checked=no` on the unread release, logged nothing and left the status where it was. With nothing still read the bookmark stays, since `checked=no` is the only other move and it takes the series' bookmark off rather than back.
- **The NovelUpdates tracker owns the site** (owner, 2026-09-21), taking the extension's tracking features, rather than letting both write (declined: double writes, and the extension's notes sync is destructive) or skipping the extension only for bound novels (declined in favour of full ownership plus auto-binding).
- **Hide, never leave inert** (owner, 2026-09-21): the taken-over settings are removed from the extension's screen by key. If an extension update renames a key, that setting reappears doing nothing until the table is updated; nothing breaks. Asking NovelSourcery for a host flag that hides them at the source is open, and is the owner's call since it is an outward-facing request.
- **Auto-binding through a Reikai-owned capability** (owner, 2026-09-21), not by splitting `EnhancedTracker`, an upstream interface read at seven upstream sites, and not by special-casing NovelUpdates, which would leave novels without manga's behaviour.
- **Amendments to the rules file** (owner, 2026-09-21): a Sources row in the depth table, and the Browse row's filter split restated per source kind. The browse ruling "the filter dispatch stays split on mechanism, a typed `FilterList` against a plugin JSON schema" keeps its mechanism and loses its premise that the mechanism follows the content type.
- **Not taken from tsundoku:** its JS plugin host and its source list (the 2026-08-02 verdict in `feature-ports.md` stands), and novels-as-manga rows, which is how its own host stores these sources.
- **The groundwork is the extension library, not a host commit.** `feature-ports.md` points at tsundoku's `b1edfc8f4` as groundwork for this item; that commit only deprecates two host-side methods. The contract the APKs compile against is `tsundoku-otaku/extensions-lib`.
- **`isNovelSource` stays off `Source`.** Every NovelSourcery APK's own `NovelSource` copy gives it a default body, so a host default beside it would leave an extension that does not override it with two defaults, which fails at the call. The kind comes from the manifest instead. `fetchPageText` is safe because that copy leaves it abstract.
- **Novel kinds are split off in `ExtensionManager`, not skipped in `AndroidSourceManager`.** Sixteen readers of the manga maps would otherwise have listed a novel APK as manga (source registration and stub rows, the Extensions lists, the update badge and its obsolete flag, backups, the crash log), so filtering at the one place results arrive covers all of them and any reader added later. An APK declaring both features loads as manga, so nothing that loads today changes (`Extension.Kind.fromFeatures`, pinned by `ExtensionKindTest`).
- **An available extension's kind is per entry, never per store.** NovelSourcery's index carries field 8000 on each of its 149 extensions and nothing at store level, as tsundoku reads it too, so a store may mix both kinds and its content types are derived from what it lists rather than stored in a column. The novel package namespace answers for an old-format index, which has no field 8000 (`Extension.Kind.fromIndex`, pinned by `StoreIndexKindTest`).
- **The LN plugin registries are fetched once and shared** (`LnRepoRegistries`): on first use, when a repo is added, and on Refresh. The plugin list used to download every registry again on each return to the tab and after each install or uninstall; Available and Updates are now derived from the cached registries and the installed plugins' records. The update check and the post-restore trust check keep fetching on their own, since both need the registry as it is now. The manga side's own repeat fetches (Mihon's refresh downloading a v2 index twice, the stored `github.com/…/raw/` redirect) are left as upstream has them (owner).
- **The Repos screen follows the download queue** (owner, 2026-09-21): one card list with the type badge shown only when both types are present, a details sheet, one Add that detects the repository's kind (owner-approved), and Mihon's five screen files deleted and manifested (owner-approved), while the store data layer stays Mihon's and untouched.
- **The two interfaces carry their own R8 keep** in `source-api/consumer-proguard.pro`. The existing rule keeps members only on types extending `Source`, and neither does; a minified build without the keep was dexdumped with both interfaces empty. `DefaultImpls` needs no keep: `Source$DefaultImpls` is already stripped empty in minified builds while extensions keep working, because extensions built with current Kotlin (NovelSourcery sets no `jvm-default` flag) call the interface's real default methods.
- **Known limits carried forward:** an extension that uses NovelSourcery's `lib/synchrony` calls a QuickJS binding Reikai does not ship (Reikai's host uses dokar3's `quickjs-kt`), the same gap keiyoushi manga extensions already have. Twelve IReader index entries are built on library 1, which IReader's own loader refuses; they show as Not loaded here too.
