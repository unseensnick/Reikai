# Library: the All chip

## Goal

One library screen that lists manga and light novels together. **All is the real view**; the Manga and Novels chips are filter predicates over that one assembled list, not two separate screens with a switch between them. A category that holds both types appears once and holds both, interleaved under one sort.

## Why

The library is the last surface where the two content types are still two screens wearing one skin. Everything below the screen is already shared (the filter, sort, query, grouping and merge-collapse kernels all run for both types), but the list itself is still assembled twice and picked between, so anything list-shaped has to be decided twice and can drift.

Building All as the real version rather than bolting a third chip onto the existing pair is a deliberate choice by the owner: a composite stitched over two states inherits both states' quirks, and every value keyed to a row (its collapse state, its count, its identity) would keep needing a per-chip answer. With All as the base, those questions have one answer each.

## The rulings that shape it

Locked by the owner; treat these as settled and do not re-litigate.

- **All is the full version, the chips are filters.** Anything that describes the list or a row in it is one value. Anything that describes a content type stays per type.
- **Counts follow the active chip.** A universal category holding 3 manga and 2 novels shows 5 under All and 3 under Manga. The count is computed over the filtered rows, not stored on the category.
- **An empty category is hidden, always** (ruled 2026-07-31, superseding the narrower "a category the chip empties is hidden"). Whether the chip, a filter, a search or plain emptiness left a category without rows, no bare header renders, on any chip. This is the novel library's old unconditional rule promoted to both types; manga's keep-when-idle behaviour and the `show_empty_categories_filtering` preference (which only manga ever read) retired with it, the key restore-skipped.
- **The manga global sort is the library-wide global sort** (ruled 2026-07-31). `library_sorting_mode` and `library_random_sort_seed` become the one global sort and Random seed both content types read; the novel keys (`novel_library_default_sort`, `novel_library_random_seed`) retire. The novel value is dropped, not migrated: a value never re-picked since the category unification can carry the old flag layout where Downloaded and TrackerMean are swapped, and the two readings are indistinguishable (see category-schema-unification.md, "sort-flag residues"), so a copy migration could silently rewrite the user's manga sort to a wrong-but-plausible mode. Cost of the clean fallback is one re-pick of a novel-only global sort; per-category overrides live in category flags and are untouched. Retired keys need a restore skip: `PreferenceRestorer`'s type check passes for keys absent from the live store (`prefs[key] is Long?` is true for null, `PreferenceRestorer.kt:103-106`), so an old backup would resurrect them without an explicit dead-key skip (precedent: `DEAD_LAST_USED_NOVEL_CATEGORY_KEY`, `:80`).
- **Collapse is per row, not per chip** (already shipped). A category collapses once, whichever chip is filtering.
- **The Default category follows the global sort on both types** (already shipped). Row 0 is universal, so an override on it could not mean two things.

## Approach

The shared layer already owns the selection, the dialogs, the settings sheet and the display config. What it does not own is **list assembly**: each provider still produces its own finished, bucketed, sorted list and the tab picks one. All-first inverts that.

**Providers yield rows, the shared layer assembles.** Each provider exposes its favourites as `List<LibraryItem>` already filtered and search-matched for its own type (that part is genuinely per type: different repositories, different source managers, different track tables). The shared layer concatenates the two lists, applies the chip predicate, **buckets into categories, then sorts within each category**, and hides the categories the predicate emptied. Bucket-before-sort is the order both sides already use, because the sort is per category: each category can carry its own override, so there is no single ordering to apply before bucketing.

**Everything is re-keyed on `EntryId`, not the raw row id.** This is the constraint that shapes the whole step. `LibraryItem.id` is the raw table id and a manga and a novel can share one (`LibraryItem.kt:27-31,41`), yet today's assembly structures are all `Long`-keyed: the bucket values, the favourites maps, the custom-info overlays, the tracker-mean maps, `memberIdsFor`, `novelRoutes`. Concatenating without re-keying produces silent cross-wiring rather than a crash, and the sharpest example is the Random sort, whose key is `Random(randomSeed + fields.id(it))` (`LibrarySortComparator.kt:79`), identical for a colliding pair.

**The chip becomes a predicate, not a branch** (DONE, step 3). `ContentType.ALL` is the default view; Manga and Novels filter the rows, and `isNovels` is gone from the tab (the state pick is a `when` with an explicit All arm).

**(Historical, resolved by step 3; kept for the rationale.)** `ALL` used to crash at composition (`behaviorFor` errored on a mixed view and the tab called it unconditionally), and once given an answer it would have silently taken every manga branch because the tab keyed on an `isNovels` boolean. That is why `isNovels` was removed **before** the chip was offered, and why the old `LibraryEngineTest` crash pin was rewritten.

**The per-type models stay live below it.** `LibraryViewModel` keeps its favourites flow and its action verbs and stays upstream-synced until phase 5 deletes it; the novel model keeps its own. Neither assembles a list any more.

**What had to move to make that possible** (all done, in step 5c):

- `groupedFavorites` and the display-derived `State` helpers retire in step 5c (`containsMerged` / `memberIdsFor` stay: they read `favoritesById`, which lives on).
- `LibraryViewModel.State.reikai` retires in step 5c. Its two remaining readers are the grouping inputs (`LibraryViewModel.kt:225`) and the toolbar title (inside `getToolbarTitle`, `:1027-1047`); both die there.
- `itemCountForCategory` became one rule (DONE, step 4): the chip-filtered bucket size, shown when the count preference is on or a search is active, on every chip.

## How the screen works (written pre-assembly; the split points and gotchas below still hold)

This section described the screen before steps 0-4 landed; the model-side pipelines it walks retired in step 5c, and the TAB renders the engine's assembly. Current wiring: `LibraryTab.Content()` builds the two models plus `LibraryEngine` over an adapter for each; it collects both adapters' neutral states, picks `libState` through a `when` over the chip (explicit All arm), and renders categories / items / counts from `engine.assembled` (chip-tagged, provider fallback for the pre-first-emission frame). The Status section is the authority on what has shipped.

Each side reaches its finished list the same way, and the split point step 1 needs is visible in both:

- **Manga.** `favorites.applyFilters(...)` produces `filteredFavorites` (`LibraryViewModel.kt:171`), stored as `LibraryData.favorites` (`:197`). Bucketing happens later, in `applyGrouping` (`:237`, defined `:393`), then `applySort` (called `:242`, defined `:417`, global sort read `:444-445`). So the rows exist, filtered and unbucketed, at `:197`. Merge-collapse already ran: it happens inside `getFavoritesFlow` (`:570`), upstream of the filter, so the split point holds collapsed representatives and not group members.
- **Novels.** `items` is the filtered row list (`NovelLibraryViewModel.kt:437`), `byId` its index (`:446`); bucketing into `byCategory` follows at `:455-461`. Same shape, same split point, and collapse likewise already ran.

**The two category lists overlap.** Manga reads `content_type IN (0,1)` and novels `IN (0,2)` (`categories.sq:35-44`, `:103-112`), so every universal row, including the system row 0, is bucketed **twice today**, once per model. Under All it must appear once. There is no union flow for the library: `CategoryRepository.getUnfiltered()` exists but has no library-facing interactor; its callers are the category manager (`CategoryActions.kt:37,89`) and `DeleteCategoryCleanup.kt:28`. Deciding what the mixed starting list is, and whether a manga-only category with no novels still shows under All, is part of assembly.

Three more things the bucketing step needs that are easy to miss:

- **Two visibility inputs, not just the category list.** `applyGrouping(categories, showSystemCategory, showHiddenCategories)` drops the system Default row unless some row is actually uncategorized, and drops hidden categories unless the user opted in. `showSystemCategory` is **derived from the rows** (`LibraryViewModel.kt:170`), so under All it must be derived from the *mixed* rows, not from one type's.
- **Category order is applied at different points.** Manga reorders after bucketing and otherwise trusts the SQL `ORDER BY sort`; novels sort before bucketing and re-apply `sortedBy { it.order }`. A list assembled from two queries is not DB-ordered, so the shared step has to order explicitly rather than inherit it.
- **The display overlay is applied late, on purpose.** Custom titles and covers are carried alongside the rows but deliberately **not** applied to them (`LibraryViewModel.kt:163-165`, and again at `:200-201`); they are applied at the display read, in `State.getItemsForCategory`, which is why `LibraryScreenState.itemsForCategory` is a function rather than a map. Assembly must keep that contract. Applying the overlay eagerly over the assembled list would either cost a whole-library map on every emission or, if skipped, silently drop the user's custom titles and covers.

The chip is a preference, `reikaiLibraryPreferences.libraryContentType`, exposed as `LibraryEngine.contentType` and written by `setContentType`, which also clears the selection because a selection can span types. Since step 3b the strip offers all three chips, `settingsFor` answers All with the merged `allSettings` binding, and `openSettingsDialog` never errors; the one remaining loud `ALL` failure is `behaviorFor`, which only explicit per-type callers reach.

## Sequenced steps

_Written as the steps were executed, and deliberately left that way. Three names used below no longer exist: the tab's `activeCategories` is now `activeBuckets`, `ReikaiDynamicCategory.displayName` is now `LibraryBucket.visualLabel`, and `LibraryDynamicGrouping.build`'s `inheritedSortFlag` parameter is gone. All three moved when the dynamic-category sentinel became a sealed type; see Decisions._

0. **Unify the global sort** (per the 2026-07-31 ruling above). The novel pipeline's `miscFlow` reads `libraryPreferences.sortingMode` / `randomSortSeed` instead of the novel keys (`NovelLibraryViewModel.kt:205-206`); `NovelLibraryAdapter` binds `globalSort` and routes `setSort`/`resetSort` exactly like `MangaLibraryAdapter` (through `SetSortModeForCategory` / `CategoryRepository`), which retires the novel model's own `setSort`/`resetSort`; the two novel preferences are deleted with a restore skip in `PreferenceRestorer`. No migration, no `versionCode` bump. This unblocks step 2's sort input and collapses `LibrarySettingsBinding.globalSort` to one shared source, resolving what were open questions 1 and 2.
1. **Rows out of the providers.** `LibraryProvider.rows` is a cold `Flow<List<LibraryItem>>` of filtered, unsorted, unbucketed rows, sourced from the two split points above. Both sides escape through state rather than a pipeline split: manga maps `libraryData.favorites`, and the novel `State` gained a `favorites` field carrying `items` in pipeline order (mirroring `LibraryData.favorites`), which proved simpler than splitting `buildState`. Each adapter's flow is `distinctUntilChanged`, since the backing state re-emits for grouping and collapse changes the rows are upstream of. Nothing consumes the flow yet, so it shipped inert.
2. **Assembly in the shared layer**, in three slices:
   - **2a (done): the chip-blind kernel.** `LibraryAssembly.kt`: `assembleLibrary(rows, categories, inputs, fields)` buckets, orders categories (order column then the sort-order pref, deduped by id), sorts per category via `sortForCategory`, and applies the empty-category rules through a temporary `keepEmptyCategories` input (manga keeps empties, novels drop them) until step 4's one rule. Values are `LibraryItem`s, never raw ids. `mixedLibraryItemSortFields` negates novel ids for the Random rank so an id-colliding pair stops tying. Pinned by `LibraryAssemblyTest` (11 cases, hand-derived from the models' rules; the models themselves resolve Injekt at init so they cannot run in a unit test).
   - **2b: the seams the engine flow needs.** Three inputs live inside the models today and need a provider-boundary answer: the **tracker mean** (manga computes it lazily inside `applySort` only when a TrackerMean sort is active, novels compute `trackerMeanScores` in `buildState`; expose as an on-demand `trackerMeans(): Map<Long, Double>` per provider and keep the engine-side lookup lazy so non-TrackerMean users never pay it), the **custom-info overlay** (assembly emits raw rows; the display read must still apply each type's overlay, so the provider exposes an overlay-apply hook the assembled `itemsForCategory` calls), and the **category list** (the engine reads `CategoryRepository.getUnfilteredAsFlow()` and chip-filters by `content_type` in code: (0,1) for Manga, (0,2) for Novels, everything for All; deciding the mixed list is assembly's job and this avoids reusing the pickers' filtered lists).
   - **2c: the engine flow and the tab swap.** `LibraryEngine.assembled`, `by lazy` (an eager flow resolves Injekt and the scope at construction and fails every `LibraryEngineTest` case), combining the providers' row flows, the category flow, and the input prefs; the tab's `activeCategories` / `activeGetItems` / `activeGetItemCount` read it instead of `libState`, while the rest of `libState` (loading, search, filters-active, index) stays adapter-sourced until step 5. **Dynamic grouping passes through the active provider's grouped state as scaffolding** (the group-by preference is per type: `group_library_by` vs `group_novel_library_by`, so the active provider's mode decides), since the per-type builders read model internals; merging dynamic groups under All (by encoded `Category.name` collision) is its own later piece. Extend `LibraryEngineTest`'s direct-construction pattern with providers feeding fixture row/category flows, then device-check that nothing visible moved.
3. **Kill `isNovels` before offering the chip** (DONE). Every consumer reads the row's own `EntryId` or an explicit `when` over the chip; the surviving per-type code is navigation (migration, update errors), the getting-started action, and the per-chip scroll/pager states. `settingsFor` / `openSettingsDialog` gained real `ALL` answers; `behaviorFor` still fails loudly on `ALL` by design, reached only by explicit per-type callers.
4. **Counts and empty categories** to the ruled behaviour, over the assembled list.
5. **Dynamic grouping into the assembly, then retire the per-type list state.** The dynamic pass-through in `LibraryEngine.assembleFor` is the last live consumer of the models' assembled lists, so it moves first. Three slices; 5a is DONE (commit `54cb12158`).
   - **5a (done): the feed seam.** Each provider exposes `dynamicGroupingFeed(groupType): DynamicGroupingFeed` (`LibraryProvider.kt`): its pre-resolved kernel inputs (DynItems plus sourceMeta / language / status / track-status maps), keyed by `EntryId` exactly as the kernel's KDoc prescribes for a mixed call (`LibraryDynamicGrouping.kt:12-17`). The resolution lives in `mangaDynamicGroupingFeed` / `novelDynamicGroupingFeed`, which the models' own builders also route through, so the rules cannot fork. The novel `State` carries `novelById` / `tracksByRep` for its feed.
   - **5b prework (done): the active-category index un-coerced, and the hopper's synthetic-id guard.** Both were pulled forward out of 5c because 5b is what makes them reachable. `LibraryScreenState` now carries the RAW `activeCategoryIndex` and the tab coerces against the list it renders (`LibraryTab.kt`), instead of each model coercing against its own category list: under All that list is neither the union nor the assembled dynamic groups, so a page past the manga model's last category resolved to the wrong category or none. The manga `State.activeCategoryIndex` is un-`private`d as one more `// RK` patch, and `lastUsedCategory` persists the raw value (readers tolerate out-of-range because the tab coerces on read). Separately, the hopper's two settings-sheet actions pass a category id only for a real category, since the sheet's tabs are swipeable and the Sort tab is reachable from either: on a dynamic group the scope must be null. That guard is defense in depth, not an observable fix, because `SetSortModeForCategory` already resolves an unknown id to the global branch; it stops the sheet claiming a scope it does not have.
   - **5b (done): one kernel call over the union.** `group_novel_library_by` retired (`DEAD_NOVEL_GROUP_BY_KEY`, restore-skipped); `LibraryEngine.assembleFor` branches on the one `groupLibraryBy` and calls `assembleDynamicGroups`, which concatenates the active providers' feeds and runs `LibraryDynamicGrouping.build` once, resolves ids through `rows.associateBy { entryId }` (dropping any with no row, then any bucket left empty), sorts every bucket with the global sort through the mixed comparator, and returns the same `List<Pair<Category, List<LibraryItem>>>` shape as the category path, so counts and the overlay read are unchanged. `AssemblyPrefs` gained the three dynamic fields via two nested combines; `LibrarySettingsBinding.groupMode` is non-null again and the `group_unavailable_all` string is gone; `displayLanguage` is `internal` and both the novel builder and the engine now share it. The spec that produced it follows.
   - **5b spec (as executed).** Group-by unifies onto `group_library_by` (`reikaiLibraryPreferences.groupLibraryBy`), retiring `group_novel_library_by` exactly like the sort and filter keys. This follows the owner's three identical rulings (sort, Random seed, filters); the visible effect is that grouping set under one chip now groups the other too, and the revert is one small commit, so do not block on re-confirming. Mechanics: delete `groupNovelLibraryBy`, add a `DEAD_NOVEL_GROUP_BY_KEY` const beside the other retired-key consts in `ReikaiLibraryPreferences`'s companion, skip it in `PreferenceRestorer`'s retired-key block, and point the novel model's `settingsFlow` (`NovelLibraryViewModel.kt`, the `groupNovelLibraryBy.changes()` input), its `groupLibraryBy` accessor, and `NovelLibraryAdapter`'s binding `groupMode` at the shared preference. Then in `LibraryEngine`:
     - Replace the `groupModes` combine input with the dynamic inputs: `groupLibraryBy`, `reikaiLibraryPreferences.collapsedDynamicCategories` and `collapsedDynamicAtBottom` become three new `AssemblyPrefs` fields. `prefsFlow` is at kotlin's 5-arg `combine` cap, so nest two combines and construct `AssemblyPrefs` in the OUTER transform where both halves are in scope (do not build a partial AssemblyPrefs inside the inner combine; the current code already uses the flat 5-arg shape at `LibraryEngine.kt:91-99`).
     - In `assembleFor`: today the pass-through branch returns before `rows` is computed, so REORDER first (compute `rows` from the active providers before any grouping branch). When `groupBy != LibraryGroup.BY_DEFAULT`: concatenate the active providers' `dynamicGroupingFeed(groupBy)` outputs (items and each map, plain list/map concatenation; EntryId keys cannot collide), call `LibraryDynamicGrouping.build` once with `inheritedSortFlag = prefs.sort.flag`, the concatenated maps, the collapse pair, `categorySortOrder`, labels via an injected `Application` context (`context.stringResource(MR.strings.unknown / .not_tracked / .group_ungrouped)`), `languageDisplay = ::displayLanguage` (make that existing private helper at the bottom of `MangaDynamicGrouping.kt` internal instead of writing a new lambda), and, ONLY when `groupBy == LibraryGroup.BY_TRACK_STATUS`, a `trackingStatusOrder` from `LibraryTrackingStatusOrder.build(trackerManager.loggedInTrackers()) { context.stringResource(it) }` (inject `TrackerManager`; identity lambda otherwise, mirroring `MangaDynamicGrouping.kt:144-150`). The kernel returns `Map<Category, List<EntryId>>`; resolve entries through `rows.associateBy { it.entryId }` and DROP ids with no row (the feed reads a `state.value` snapshot that can lag the `rows` emission by a tick; the next emission reconciles), sort every bucket with the existing mixed comparator under the global sort (dynamic groups have no per-category override), and emit the same `LibraryAssembled` shape: counts rule unchanged, and the `items` lambda MUST apply `byType[...].overlaid(item)` exactly like the category path does (`LibraryEngine.kt:167-169`), or custom titles/covers vanish in dynamic modes. The reorder therefore hoists not just `rows` but also `searchActive`, `showCounts`, `byType`, and `means`/`fields` above the grouping branch (the dynamic buckets sort through the same `mixedLibraryItemSortFields`; the comparator takes `prefs.sort` and `prefs.seed`). After the drop-missing reconciliation, drop any bucket left empty, matching the category path and step 4's rule. Perf note: the feeds re-resolve whole-library metadata on every assembly emission (today's models re-group on a narrowed distinct key); accept for now, and memoize the feed on (rows identity, groupType) only if profiling shows it. Two deliberate parity notes: the dynamic path does not consult `showHiddenCategories` (synthetic groups cannot be hidden) and passes `categorySortOrder` to the kernel rather than `reikaiSortCategories` (the kernel orders groups itself); both match today's per-type behaviour. Under a single-type chip only that type's feed is in the union, which reproduces today's grouping.
     - Sheet cleanup: `LibrarySettingsBinding.groupMode` reverts to non-null (both adapters pass the shared pref), `LibraryEngine.allSettings` stops nulling it, and `LibrarySettingsSheet.GroupPage`'s null branch plus the `group_unavailable_all` string (in `i18n/src/commonMain/moko-resources/base/strings.xml`, NOT the app module) are deleted. No migration cleans the retired `group_novel_library_by` value out of live stores; that matches the sort/filter retirements (the restore skip is the only guard), and a lingering unread value is harmless.
     - Verify: the kernel is already unit-pinned (`LibraryDynamicGroupingTest`); device-check by-source and by-tag under all three chips (by-source must show manga and novel sources as separate groups under All; by-tag must merge a tag shared by both types into one group) plus Ungrouped under All (one bucket holding both types). Collapse a dynamic group under All and confirm it collapses once (keys are the encoded `Category.name`, shared by design).
   - **5c (done): the retirement.** The models are row providers plus action verbs now. Both traps were handled first: `isLoading` clears in the manga model's FIRST pipeline collect, and `LibraryScreenState.overlayKey` carries each type's custom-info map identity so an overlay edit is not conflated away (device-verified: a custom title set while the library was on screen reached the row without a restart). `randomEntry` and the toolbar title moved to the engine and the tab; the three pager states size off per-chip snapshots. Deleted: the manga model's second combine, `groupedFavorites` and its display helpers, `State.reikai` and its writer, `applyGrouping` / `applySort` / `buildMangaDynamicGrouping` / `reorderReikaiCategories`, plus the novel model's grouping branch, its list helpers, the `GroupingInputs` wrapper and `settingsFlow`'s group-by input. `ReikaiLibraryState` still carries four now-unread fields (`groupLibraryBy`, `collapsedDynamicAtBottom`, `categorySortOrder`, `showHiddenCategories`); removing them means reshuffling an indexed 14-arg combine, so it is left as its own cleanup. The spec that produced it follows.
   - **5c spec (as executed).** The models stop grouping and sorting; they remain row providers plus action verbs. Two runtime traps come FIRST, because missing either bricks the screen while compiling clean:
     - **`isLoading` must find a new writer.** Its only set-to-false today sits inside the second combine's `collectLatest` (`LibraryViewModel.kt:245`), which 5c deletes; the default is `true` (`:940`), so deleting without replacement shows the loading spinner forever on every chip. Move the write into the FIRST combine's `collectLatest` (beside the `libraryData` copy, `:206-210`) or derive it as `!libraryData.isInitialized`. Loading then lifts one tick before `assembled` first emits; that single empty-list frame is acceptable (`isLibraryEmpty` is false, so it renders the list branch with zero categories, not the empty screen).
     - **A custom-info edit must still re-fire assembly.** Today the neutral states carry bound function references (`itemsForCategory = this::getItemsForCategory`), which change identity on every model State change, so a customInfo-only edit reaches the engine. Once those fields die, every remaining neutral field is unchanged by a customInfo edit, the `stateIn` conflates, and the edit never reaches the screen (the rows deliberately exclude the overlay). Add an `overlayKey: Any?` field to `LibraryScreenState`, mapped from each model's customInfo map reference, so overlay edits change neutral-state equality; nothing reads the field, it exists to defeat conflation. Note this path has never been device-exercised, so it will not be caught by hand.
   - Then work through the consumers before deleting:
     - **Random entry moves to the engine.** `randomEntry` is declared on `LibraryBehavior` (`LibraryBehavior.kt:34`, which drops to 16 action members), and `engine.randomEntry` fans out to it; the implementations read the models' grouped state (`MangaLibraryAdapter.kt` via `getItemsForCategoryId`, `NovelLibraryAdapter.kt` via `randomItemId`). Reimplement `engine.randomEntry(contentType, categoryId)` over `assembled.value`: resolve the `Long` id to a category via `categories.find { it.id == categoryId }` (null when absent; a dynamic group's synthetic id is only valid for the current emission), a category pick is `itemsFor(category).randomOrNull()?.entryId`, whole-library is the union of every bucket DISTINCT by `entryId` (a multi-category entry appears in several buckets). When `assembled.value` is null or its `chip` does not match `contentType` (the flow lags a chip flip by one emission), return null; the tab already shows the no-entries snackbar for null. Two deliberate notes: the whole-library pick now draws from what is on screen (rows in hidden or emptied categories are excluded; manga's old path drew from raw `favorites`), and touching `assembled` resolves Injekt, so `randomEntry` leaves `LibraryEngineTest`'s direct-construction coverage and is verified on-device instead. Delete the `LibraryBehavior` member and both implementations, plus the manga model's now-caller-less `getRandomLibraryItemForCurrentCategory` (`LibraryViewModel.kt:802-805`).
     - **The toolbar title moves off the manga model.** `LibraryTab` still calls `state.getToolbarTitle` (the manga model) on every chip; rebuild it in the tab as a pure function over `assembled` plus `display`, keeping the manga rules exactly (`LibraryViewModel.kt:1027-1047`): category name is the default-category label for the system row, `ReikaiDynamicCategory.displayName` otherwise; the name replaces the "Library" title when `display.reikai.showCategoryInTitle || !display.showCategoryTabs`; the count shows only when `display.showItemCounts`, and is the page category's bucket size when tabs are off, else the whole-library count. One deliberate change: the whole-library count becomes the distinct-entry count across the assembled buckets (it was `libraryData.favorites.size`, which under All would count only manga). This was step 6's first item; it lands here because `getToolbarTitle` reads `displayedCategories`, which is about to die.
     - **The active-category index: already done** in the 5b prework above. What remains here is only deleting each model's now-unread `coercedActiveCategoryIndex` and `activeCategory` along with `displayedCategories`.
     - **The pager states.** `mangaPagerState` / `novelPagerState` read `state.displayedCategories.size` and the model states' coerced indexes directly (`LibraryTab.kt:225-230`), not `libState`. Do NOT point all three `pageCount` lambdas at `activeCategories.size`: Compose clamps a pager's `currentPage` when its `pageCount` shrinks, so an inactive chip's pager would lose its position whenever the active chip has fewer categories. Keep three pager states, each counting a per-chip `remember`ed snapshot updated only while its own chip is active (`if (chip == X) snapshot.intValue = activeCategories.size`); the manga pager seeds from the tab-coerced restored index, novel and All seed 0 (the novel model never restored `lastUsedCategory`, unchanged).
     - **Then delete:** manga's second pipeline combine (`LibraryViewModel.kt:213-251`), `State.groupedFavorites` and the RK-fenced display helpers (`displayedCategories`, `groupedFavoritesById`, `coercedActiveCategoryIndex`/`activeCategory` as coerced values, `getItemsForCategory(Id)`, `getItemCountForCategory`, `getToolbarTitle`), the `State.reikai` field with its writer (`:302-307`; its two remaining readers die with the combine and the title), `applyGrouping`/`applySort`/`reorderReikaiCategories`'s model-side use, and `buildMangaDynamicGrouping`; the novel `buildState` ends at its row split (delete the `grouped` computation, `groupedFavorites`, `displayedCategories`, `groupedById`, coerced index/`activeCategory`, `getItemsForCategory`, `getItemCountForCategory`, `itemIdsForCategory`, `randomItemId`, and `buildNovelDynamicGrouping`), and its pipeline sheds the now-dead grouping inputs: the `GroupingInputs` wrapper with its two collapse inputs (`NovelLibraryViewModel.kt:158-162`) and `settingsFlow`'s group-by input with `LibrarySettings.groupBy`, or every collapse tap keeps rebuilding the whole filtered novel list for nothing (the doc's emission-coupling gotcha); keep `favorites`, `favoritesById`, `trackerMeans`, `novelById`, `tracksByRep`, `customInfo`/`withOverlay`, `novelRoutes`/`routeFor`, and `containsMerged`/`memberIdsFor` (`favoritesById`-based, used by selection verbs). `LibraryScreenState` drops `categories`/`itemsForCategory`/`itemCountForCategory` and renames the index field raw; the tab's `?: libState.*` fallbacks become empty defaults (the pre-first-emission frame sits behind `isLoading` on cold start). Before deleting each symbol, grep for callers the lists above may have missed; anything found joins the rework, not a shim.
     - Verify: compile, the full suite, then the wide device pass (three chips times both view modes; open, random, select-all, collapse, search each spot-checked once).
6. **Parity closeout.** The toolbar title and the hopper long-press scope both closed earlier (5c and the 5b prework respectively). What remained was search, now unified: `LibraryQueryMatch.kt` is the shared query kernel (`LibraryQueryFields<T>` + `libraryQueryMatches`), built to the same shape as `LibraryFilterFields`, and both libraries bind it through `libraryItemQueryFields`. Mihon's grammar (lexer / parser / AST) stays upstream and unpatched; only the evaluator is Reikai's, so `mihon/feature/library/QueryNodeExtensions.kt` is deleted and manifested. Novel rows now carry `sourceName`, `sourceLanguage` and `notes`, which is what lets the shared fields read off the row. Three seams: the source key is a String on both sides (numeric id for manga, plugin slug for novels), and `fetchInterval` / `nextUpdate` are null for novels, which makes those terms false BEFORE negation so an inapplicable comparison never pulls a novel into a mixed list from either direction (device-verified: under All, `nu<2030-01-01` and `-nu<2030-01-01` both return manga only). Manga's EXH tag grammar is ORed with the AST rather than routed between, retiring the `isPrefixQuery` sniff. Novel `id:`/`src:` become `id=`/`srcid:`, the manga spellings. `chapter:` (alias `ch:`) then landed as a net-new field on both types: one `// RK` line adds `CHAPTER` to `MangaField` as `fieldOnly`, so a bare word never pays a chapter lookup; `QueryNode.chapterSearchTerms()` walks the parsed tree so only the terms actually typed are resolved (quoting and negation already handled); each side runs its own `LIKE` projection (`getMangaIdsWithChapterNameLike` / `getNovelIdsWithChapterNameLike`, query-only, no `.sqm`) once per query change, manga off the debounced query slot and novels inside `buildState`, and the kernel tests set membership per row. Negation falls out of the AST's `NotNode`, which the Tsundoku implementation it was modelled on cannot do. A chapter hit on a merge-collapsed member surfaces the representative only, by ruling. Finally, search matches custom entry info: `LibraryQueryOverlay` is a neutral holder of the five overridable searchable fields (status is overridable but has no query field), each side maps its own custom-info row onto it, and the query fields read `overlay[id]?.field ?: sourceValue`. That is a map lookup per field rather than a copied row, because the rows themselves stay override-free on purpose: filter, sort and grouping all still read the source values. An override REPLACES the source value for search (device-verified: after renaming, `title:myriad` matches nothing while `desc:myriad` still does). The grammar is documented for users in [library-search.md](../../library-search.md).

## Key files

Paths are from the repo root, which **is** `.../yokai-y2k/app`. App-module sources therefore live under `app/src/main/java/`, and the sibling reference clones are at `../refs/`, outside the repo.

Under `app/src/main/java/reikai/presentation/library/`:

- `LibraryEngine.kt`: the shared engine, a Voyager `ScreenModel`. Owns selection, dialogs, display config, the chip, collapse, and the settings-description lookup. **This is where assembly lands.**
- `LibraryProvider.kt` and `LibraryBehavior.kt`: the per-type seam (`LibraryBehavior` holds the action members; `LibraryProvider` adds `contentType`, `settings`, `rows`, `trackerMeans`, `overlaid`, `dynamicGroupingFeed`). Providers answer about entries and perform writes; they do not open dialogs and do not own collapse.
- `LibraryScreenState.kt`: the neutral per-type state the tab renders. Only genuinely per-type content lives here; library-wide values belong on the engine.
- `MangaLibraryAdapter.kt`, `NovelLibraryAdapter.kt`: the two adapters, constructed by the engine's own factory.
- Type-neutral shared kernels, all unit-tested: `LibraryItemFields.kt`, `LibraryFilter.kt`, `LibraryQueryMatch.kt`, `LibraryDynamicGrouping.kt`.
- The two grouping builders, deliberately side by side: `MangaDynamicGrouping.kt` and `novels/NovelDynamicGrouping.kt`.

Elsewhere:

- `domain/src/main/java/reikai/domain/library/LibrarySortComparator.kt`: the shared comparator both types sort through. `CategorySortOverride.kt` beside it owns the per-category override rule.
- `app/src/main/java/eu/kanade/tachiyomi/ui/library/LibraryTab.kt`: the consumer. `isNovels` is gone (step 3); the remaining per-type code is navigation (migration, update errors), the getting-started action, and the per-chip scroll/pager states, all keyed by explicit `when`s over the chip or the row's `EntryId`.
- `app/src/main/java/eu/kanade/tachiyomi/ui/library/LibraryViewModel.kt`: Mihon's, live and `// RK`-patched, deleted and manifested at the end of the programme.
- `app/src/main/java/reikai/presentation/library/novels/NovelLibraryViewModel.kt`: Reikai's, the novel twin.
- Tests: `app/src/test/java/reikai/presentation/library/`.

## Status

**All steps (0 through 6) are built and device-verified: the All chip is live with the ruled count and empty-category behaviour, dynamic grouping runs under All, the per-type list state is retired (5c), and the search grammar is one kernel (6). This doc is the complete record of the finished library surface.** The step 4 rule: counts are the chip-filtered bucket size, preference-gated (search overrides) on every chip; an empty category is always hidden (the owner corrected the first cut, which showed truly-empty categories when idle), and the show-empty-while-filtering preference and toggle retired with the manga model's keep-when-idle path. 3a killed `isNovels`: the tab picks state through a `when` with an explicit All arm (combined loading/emptiness/filters, own scroll and pager states, interim null counts until step 4), the engine fans out search and the page index and exposes a chip-free `globalSort`, assembled output is chip-tagged so a flip never renders the previous chip's list, migration hides on a mixed selection, and Merge requires two entries of one type. 3b unified the filter preferences onto the manga keys per the owner's 2026-07-31 ruling (Tsundoku-corroborated: one shared filter set, content kind as a predicate): the novel filter keys and the novel merge-icons toggle retired with a prefix restore skip, the shared include/exclude filter moved to a `sharedSets` registry bucket scrubbed against the union, `settingsFor(ALL)` answers with the manga binding plus a union category list and a null group mode (the Group tab explains under All; per-type group modes are ignored there so a grouped Manga view can never crash a flip to All), and the chip strip offers all three. The emulator pass covered the mixed list, the sheet under All, and a filter set under All reaching both other chips. The Fold pass (2026-07-31, real library) covered all three chips in both view modes (single-list and the tabbed pager paging across assembled categories), per-row routing under All (a novel opens the novel screen), TrackerMean through both seams (scored manga cluster at the ascending tail of the mixed Default; the scored novel sorting after the unscored one), and the shared tracker filter reaching novels (a Kitsu filter under All yields exactly the Kitsu-bound entries of both types). The custom-info overlay through `overlaid` has since been exercised on-device too (a live rename repaints the library card). The step 2 pass confirmed both chips render identically off the assembly, by-source grouping passes through and returns cleanly, and a search drops emptied categories with the model-side counts matching the assembled items. Not exercised on-device: TrackerMean sort through the seam (no tracker logins on the emulator; the scoring rule is shared code and the kernel is unit-pinned) and the custom-info overlay through `overlaid` (no custom titles in the emulator library; both States route their own display read through the same `withOverlay`). Worth one eye each on the Fold pass (the sort ruling above resolved what gated step 2). The step 0 pass confirmed: a sort picked under the Novels chip writes `library_sorting_mode` (the retired novel key stays untouched), both lists and headers re-sort live, and picking Random under Novels regenerates the shared Int seed. The override path passed too: with categorized display on, a novel category header's sort set an override that re-sorted only that category, left the global preference unwritten, survived a global-sort change, and cleared through Reset to global sort. The preparatory phases are done and device-verified: the behaviour seam, one settings sheet, and the two phase 3 prerequisites (collapse as one library-wide value, and the novel grouping builder extracted so the two cannot drift). The full record of those, and of the shared-pipeline work under them, is [content-layer-library-surface.md](content-layer-library-surface.md); the programme-level design is [content-layer-architecture.md](content-layer-architecture.md).

## Decisions & tradeoffs

- **All-first rather than a third chip.** Chosen by the owner as a purpose-built rebuild. The alternative, merging two independently assembled lists at the end, produces a correct-looking mixed list while leaving both pipelines forked, which is the failure this programme exists to remove.
- **Providers keep their own filtering and search.** They read different repositories and resolve different source managers, so filtering at the provider is honest; only assembly is shared. The row type is Mihon's `LibraryItem` on both sides, and novels already convert to it before filter and sort.
- **Interactors and repositories stay Mihon's.** The takeover stops at orchestration. Any step that starts reimplementing what `setReadStatus` or `DownloadManager` does has gone too far.
- **The dynamic bucket's negative-id sentinel was replaced by a sealed type** (2026-08-10, after this plan shipped). The steps above build a dynamic group as a `Category` with a negative id, and that is how it worked here. It has since been retyped: five hand-written guards were all that kept such a category out of a category-scoped write, and the first bucket of every grouping sat on id -1, the value `NovelUpdateJob` reads as "update the whole library". A library section is now a sealed `LibraryBucket`, real category or dynamic group, and only the real case can answer a `Category`. The persisted collapse-key format is unchanged. Full record in `library-screen-carry.md`.
- **A source bucket stays per content type.** Manga encodes a numeric source id in the dynamic group's key where novels encode a plugin slug, so the two never merge into one bucket, which is correct: a manga source and a novel source are different sources. Pinned by `LibraryDynamicGroupingTest` over a mixed list.
- **No schema or backup change.** Categories are already one table with a `content_type` column and one id space; rows already carry real positive ids behind a neutral `EntryId`. Assembly is code-only.
- **The global sort unified by retiring the novel keys, not by migrating them** (ruled 2026-07-31; the fallback question and its Random-seed twin, plus whether the Sort tab stays per-type, were open questions until then). The clean fallback was chosen over a value-adopting middle path: adopting the novel value only when it decodes outside the ambiguous {Downloaded, TrackerMean} set was considered and declined, since it keeps a two-preference read path alive for one release to save one re-pick.

## Gotchas worth knowing before starting

- `LibraryEngine`'s preference-backed flows are `by lazy` on purpose: `LibraryEngineTest` constructs the engine directly with mocked providers, and eager properties resolve Injekt and a coroutine scope at construction, which broke every case. Any new preference-backed member needs the same treatment.
- The engine outlives the composition, so the adapters must be constructed by the engine's own factory, never `remember`ed separately, or a tab switch hands the tab one pair while the engine dispatches through another.
- `isLibraryEmpty` is counted **after** filters on both content types, so anything keying on it needs the no-active-filter guard the tab already applies, or a filtered-to-nothing library reads as empty.
- Nothing validates `content_type` on the category junction tables, so a category assigned to the wrong type writes a row that appears in no picker and can never be removed. This is why the change-categories flow intersects the per-type assignable lists rather than merging them.
- **`itemsForCategory` and `itemCountForCategory` are functions on the neutral state for a reason**, not a precomputed map: they apply the per-entry custom-info overlay lazily, only for the categories actually rendered. Keep them lazy through the assembly, or a large library pays a whole-library map on every emission.
- **Merging the two flows couples their emission rates.** Each side has its own download-cache tick, and one completed chapter download would re-run assembly over both types' rows. The guards are also unequal: the manga combine has `distinctUntilChanged` (twice, one on a deliberately narrowed grouping key), the novel combine has none, and each side debounces search on its own 250 ms timer, so a keystroke burst reaches a naive assembler twice. Hoist the query above both, and do not let the weaker guard become the shared one.
- **Keep collapse out of the row flows.** The novel pipeline currently takes the dynamic-collapse preference as a combine input (`NovelLibraryViewModel.kt:164-168`) while the manga side reads collapse off the engine's display state and never re-enters its pipeline. Downstream of assembly, that asymmetry would make every collapse tap rebuild both content types.
- **Several verbs assume a single type today and would misfire on a mixed selection:** migration flattens `rawId` across two id spaces (`LibraryTab.kt:413`), and Merge's enable gate counts the whole selection (`:425`), so a mixed two-plus-two would create two separate groups from one gesture. Neither is a crash, so neither announces itself.
- **`MergeGroupRepository` rejects `ContentType.ALL` explicitly** (eleven `error(ALL_UNSUPPORTED)` sites in `reikai/data/merge/MergeGroupRepositoryImpl.kt`, one of them the enum-to-string mapper the other ten route through). That is correct and should stay: merge groups are per type, and only the providers should reach it, each with its own hardcoded type. Assembly must never pass the chip down.
- Library-layer tests run under `:app:testDebugUnitTest`, not `:domain:test`.
- Device passes need both view modes (the tabbed pager and the single-list hopper) and, once All exists, all three chips.
