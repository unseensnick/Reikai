# Compose + Voyager port status

**Superseded by the Mihon rebase (2026-06).**

This document tracked Compose-migration debt on the Yōkai-based codebase (which screen had migrated from a Conductor `*Controller` to Compose + Voyager). That migration is moot on the Mihon base: Mihon is Compose + Voyager throughout, so there is nothing to migrate from.

Reikai's screen conventions on Mihon live in [.claude/rules/compose-port.md](../../.claude/rules/compose-port.md). The rebase plan and per-phase status live in [ROADMAP.md](../../ROADMAP.md) and the `mihon-rebase` memory.

The historical Yōkai-era snapshot is preserved in git history on the `design/library-compose` branch.
