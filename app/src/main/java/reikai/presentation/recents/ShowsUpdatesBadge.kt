package reikai.presentation.recents

/**
 * A tab that draws the unread-updates badge and clears the count while it is open. Named rather than
 * tested for by class, because which tab shows Updates is now a setting: the badge asks what a tab
 * does, and a second type test would have gone stale the moment a third tab could answer for it.
 */
interface ShowsUpdatesBadge
