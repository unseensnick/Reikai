{
  "name": "block reading the signing keystore properties through the shell",
  "stdin": { "tool_input": { "command": "cat keystore.properties" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "secret file"]
}
