{
  "name": "block perl -pi on a git hook source",
  "stdin": {
    "tool_input": {
      "command": "perl -pi -e s/a/b/ .githooks/pre-commit"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "shell write"
  ]
}
