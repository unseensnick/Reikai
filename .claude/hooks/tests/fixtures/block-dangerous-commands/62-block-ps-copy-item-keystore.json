{
  "name": "block Copy-Item keystore.properties elsewhere",
  "stdin": {
    "tool_input": {
      "command": "Copy-Item keystore.properties C:/tmp/k"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "deny",
    "secret file"
  ]
}
