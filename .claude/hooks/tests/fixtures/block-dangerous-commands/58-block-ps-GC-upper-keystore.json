{
  "name": "block GC keystore.properties (verb in upper case)",
  "stdin": {
    "tool_input": {
      "command": "GC keystore.properties"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "deny",
    "secret file"
  ]
}
