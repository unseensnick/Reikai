{
  "name": "block gh pr --repo <repo> merge",
  "stdin": {
    "tool_input": {
      "command": "gh pr --repo unseensnick/Reikai merge 71 --squash"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "owner's call"
  ]
}
