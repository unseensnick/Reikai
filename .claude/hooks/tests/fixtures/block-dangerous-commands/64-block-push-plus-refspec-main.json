{
  "name": "block git push origin +main",
  "stdin": {
    "tool_input": {
      "command": "git push origin +main"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "deny",
    "main"
  ]
}
