{
  "name": "block git.exe push origin main",
  "stdin": {
    "tool_input": {
      "command": "git.exe push origin main"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "deny",
    "main"
  ]
}
