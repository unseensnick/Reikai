{
  "name": "block push --mirror, which overwrites every remote branch",
  "stdin": {
    "tool_input": {
      "command": "git push --mirror origin"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "every branch"
  ]
}
