{
  "name": "block cp into the hooks directory",
  "stdin": {
    "tool_input": {
      "command": "cp /tmp/x .claude/hooks/y.sh"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "shell write"
  ]
}
