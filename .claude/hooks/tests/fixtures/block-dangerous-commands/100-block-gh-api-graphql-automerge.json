{
  "name": "block a gh api graphql enablePullRequestAutoMerge mutation",
  "stdin": {
    "tool_input": {
      "command": "gh api graphql -f query='mutation { enablePullRequestAutoMerge(input: {pullRequestId: \"PR_x\"}) { clientMutationId } }'"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "owner's call"
  ]
}
