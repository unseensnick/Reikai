{
  "name": "block Remove-Item -Recurse -Force C:/",
  "stdin": {
    "tool_input": {
      "command": "Remove-Item -Recurse -Force C:/"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "deny",
    "recursive PowerShell delete"
  ]
}
