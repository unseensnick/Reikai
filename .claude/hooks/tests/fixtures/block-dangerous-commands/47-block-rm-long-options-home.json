{
  "name": "block rm --recursive --force ~",
  "stdin": {
    "tool_input": {
      "command": "rm --recursive --force ~"
    }
  },
  "expect_exit": 2,
  "expect_stdout_contains": [
    "deny"
  ]
}
