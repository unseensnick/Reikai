{
  "name": "allow staging a hook script",
  "stdin": {
    "tool_input": {
      "command": "git add .claude/hooks/x.sh"
    }
  },
  "expect_exit": 0,
  "expect_stdout_not_contains": [
    "permissionDecision"
  ]
}
