{
  "name": "block reading google-services.json with the gc alias",
  "stdin": { "tool_input": { "command": "gc app/google-services.json" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "secret file"]
}
