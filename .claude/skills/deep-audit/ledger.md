# Deep-audit ledger

Findings a `/deep-audit` run refuted, so the next run does not raise them again. A row holds only while
its reason is still true; once the code or the ruling behind it changes, the finding may come back with
the new evidence. Rows are appended by the skill and committed by the owner.

| Range | Location | Claim | Refuted because |
|---|---|---|---|
| b47d2d726..d7d9c4eac | `reikai/novel/update/LnPluginUpdateChecker.kt:1040` | Novel plugin updates now match by registry-declared id across repos, so any added repo can offer a same-id script from its own URL | Matching plugin updates by registry id across repos is the documented design, copied from the reference app (816b8127b). |
| b47d2d726..d7d9c4eac | `CLAUDE.md:31` | CLAUDE.md now says Injekt survives only for the extension contract, but app-side tracker code still reads Json through injectLazy | The omission of the tracker Json reads predates the range; the range only dropped the novel-reader clause. |
| b47d2d726..d7d9c4eac | `ROADMAP.md:15` | Road B's '21 marker lines across seven of them' does not re-derive against the download files | The count holds: ChapterFilterDownloaded.kt is the seventh file, outside the two directories the finding counted. |
| b47d2d726..d7d9c4eac | `androidTest/java/reikai/presentation/reader/web/NovelWebDocumentTest.kt:874` | Renderer-specific copies of rules the contract already runs over both renderers were left in place, one of them extended in range | Same shape as the earlier refuted row for NovelWebDocumentTest.kt:824: the per-renderer copies sit one layer below the contract test. |
| b47d2d726..d7d9c4eac | `main/assets/novel-web/reader.js:438` | The web page counts text that CSS hides, and a line that lands inside it falls back to the fraction instead of the nearest line | Backwards: both renderers count source-hidden text alike, the web one over text nodes and the native one over what the TextView holds. |
| b47d2d726..d7d9c4eac | `eu/kanade/presentation/category/components/CategoryListItem.kt:64` | New move-menu parameters carry defaults no call site ever uses | The defaults copy the shape of the RK parameters beside them; single-caller defaults are not a defect. |
| b47d2d726..d7d9c4eac | `eu/kanade/tachiyomi/data/backup/restore/restorers/PreferenceRestorer.kt:66` | Source-preference restore runs the app-wide dead-key carries against a source's own store | Only an extension storing an app-wide key in its own source store could trigger it, and none does. |
| b47d2d726..d7d9c4eac | `eu/kanade/tachiyomi/data/backup/restore/restorers/PreferenceRestorer.kt:146` | Restoring an old backup with the NSFW switch on never widens content warnings | Upstream's own semantics: its migration narrows only when the switch is off, and its backups never carry the switch. |
| b47d2d726..d7d9c4eac | `eu/kanade/tachiyomi/ui/browse/source/browse/BrowseSourceViewModel.kt:130` | An outdated extension can still crash the catalogue on open, through getFilterList, which the LinkageError fix left unguarded | The getFilterList path is upstream's own shape and predates the range; the range's fix covered the open path it named. |
| b47d2d726..d7d9c4eac | `eu/kanade/tachiyomi/ui/reader/ReaderViewModel.kt:1231` | Chapter sheet re-reads every merged member's chapters and disk-checks the whole pool on each download-queue change | The pooled disk check is needed for correctness, the novel side does the same, and it all runs off the main thread. |
| b47d2d726..d7d9c4eac | `mihon/app/di/AppGraph.kt:220` | AppGraph comment says the metadata trio is reached through Injekt rather than the graph, but getFlatMetadataById has graph readers | The comment explains why the trio are graph accessors (the registrar reads them), which is true; an extra reader does not mislead. |
| b47d2d726..d7d9c4eac | `mihon/app/di/injekt/MetroInjektRegistrar.kt:47` | The Injekt registrar still lets any installed extension get TrackPreferences and TrackerManager (tracker tokens and passwords), wh | The exposure predates the range, which narrowed it; the only reader is the built-in MangaDex source. |
| b47d2d726..d7d9c4eac | `reikai/data/novel/NovelChapterRepositoryImpl.kt:137` | setBookmarkBulk catches Exception around a suspending transaction, swallowing CancellationException | The transaction body has no real suspension point, so no cancellation can be swallowed there. |
| b47d2d726..d7d9c4eac | `reikai/data/novel/update/NovelUpdateNotifier.kt:122` | Two new manga-twin markers added in range name no pin | Neither is an unpinned twin: the title chop reads one shared constant and the other half shares its kernel. |
| b47d2d726..d7d9c4eac | `reikai/domain/novel/NovelPreferences.kt:228` | Mark-read-on-skip twin marker names no pin although both halves' skip behaviour changed in range | The marker predates the range, and the in-range edit only made the novel write non-cancellable; the gates still match. |
| b47d2d726..d7d9c4eac | `reikai/domain/novel/model/Novel.kt:110` | Novel.hasCustomCover's default argument, rewritten in this range, has no caller | Keeping the default is a ruled holdout recorded in metro-di-migration.md. |
| b47d2d726..d7d9c4eac | `reikai/novel/content/NovelCodeSnippet.kt:20` | A stored JS snippet with no id gets a new id on every decode and runs again on every unrelated settings change | The snippets decode in their own combine over three inputs, so an unrelated setting change never re-decodes them. |
| b47d2d726..d7d9c4eac | `reikai/presentation/browse/extension/ExtensionsProvider.kt:168` | A failed legacy plugin with no recorded id is listed twice, and no test covers that path | Needs a restore, a transient repo failure and a second repo listing the same id at once, and clears on the next load (owner, refuted). |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/NovelReaderViewModel.kt:494` | The model accepts top-line reports during an unsettled landing, which takeReport refuses | landingOf only uses the line for the anchor chapter, which an unsettled landing always counts, so no stale line is used. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/NovelTextViewport.kt:1214` | With text selection on, a tap on a native image's Retry pill likely toggles the chrome instead of retrying | Same mechanism as F176: a Retry pill that selection mode cannot tap; ruled with text selection's link handling. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/NovelWebViewport.kt:610` | Rounding before the threshold compare marks a chapter read half a percent early, and a 100% setting fires short of the end | Rounding before the threshold is the ruled tsundoku port, applied identically by both renderers. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/ReadAloudController.kt:253` | Other read-aloud commands during a chapter handoff still drop the pending chapter | A previous press during a handoff going back into the old chapter is what previous means; nothing is lost. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/settings/ReaderSettingsSheet.kt:53` | Novel half of the shared reader settings sheet reads NovelPreferences directly inside composables | Same as the earlier refuted row for NovelReaderSettingsPages.kt:124: both paths write the same preferences, so nothing diverges. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/text/NovelChapterFinish.kt:56` | NovelChapterFinish drops SetNovelReadStatus's Result, so a failed mark still pushes to trackers, trims downloads and stays latched | Dropping the result is the manga twin's own upstream shape, which the KDoc names deliberately. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/text/NovelImageGetter.kt:237` | Native text mode draws a Retry pill on a failed picture that cannot be tapped when text selection is on | With selection on, links are not tappable by design (ArrowKeyMovementMethod); the pill works with selection off. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/text/NovelOpenLanding.kt:53` | Opening an unread chapter whose saved position is at or above the threshold marks it read with no reading | Opening at a saved position at or above the read threshold marking the chapter read is the recorded threshold rule. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/text/ParagraphShape.kt:30` | A left or right margin change now rebuilds every chapter in the window from HTML, even when none of them has a picture | The redraw on any side-margin change is deliberate, recorded in dd4a18397 and pinned by ParagraphShapeTest. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/web/NovelWebSnippets.kt:17` | Switching a JavaScript snippet off, then on again, has no effect on the open page until the next chapter load | JavaScript cannot be un-run without a reload; a disabled snippet stops reaching anything new, which is the recorded behaviour. |
| b47d2d726..d7d9c4eac | `test/java/eu/kanade/tachiyomi/data/backup/restore/restorers/PreferenceRestorerTest.kt:36` | The PreferenceRestorerTest KDoc claims coverage the file does not have | The KDoc explains why retired keys need handling and names existing restorer behaviour; it claims no coverage. |
| b47d2d726..d7d9c4eac | `test/java/eu/kanade/tachiyomi/ui/reader/loader/HttpPageLoaderTest.kt:35` | HttpPageLoaderTest runs on the real IO dispatcher and a 5-second wall-clock verify instead of runTest | Ported verbatim from upstream mihonapp/mihon#3770, which has the same shape. |
| b47d2d726..d7d9c4eac | `test/java/mihon/core/migration/migrations/RetireLegacyNovelReaderKeysMigrationTest.kt:199` | runsAfterReadAloudButtonMigration compares version floats but its name claims the switch is still readable | Order comes from MigrationJobFactory sorting by version, identical to upstream, so comparing versions is the real contract. |
| b47d2d726..d7d9c4eac | `test/java/mihon/domain/source/interactor/UpdateMangaFromRemoteTest.kt:31` | UpdateMangaFromRemoteTest passes on any Exception, not the wrapped LinkageError | Every other collaborator is a relaxed mock, so only the LinkageError path can throw, and the named mutation fails the test. |
| b47d2d726..d7d9c4eac | `test/java/reikai/domain/novel/NovelChapterAggregationTest.kt:239` | The shared ranking kernel is tested only through hand-kept twin tests in the two aggregation suites | The rule already exists once as the sourcePriority kernel, which content-layer.md ranks above a conformance test. |
| b47d2d726..d7d9c4eac | `test/java/reikai/domain/novel/model/NovelReadingOrderTest.kt:102` | The reading-order tie-break is pinned only for novels, and the conformance fixture has no ties | Both halves predate the range, and the manga tie-break is upstream Mihon's own shape. |
| b47d2d726..d7d9c4eac | `test/java/reikai/presentation/browse/extension/LnPluginManagerViewModelTest.kt:90` | The repo-change test checks a mock call count where the model's state would show the result | The behaviour pinned is the fetch count itself, which state cannot show, so testing.md's call-count rule does not apply. |
| b47d2d726..d7d9c4eac | `test/java/reikai/presentation/recents/RecentsEngineTest.kt:1081` | The downloaded-filter recents test's assertion restates its own wait predicate | Not vacuous: a regression leaves first{} waiting and runTest fails on its timeout. |
| b47d2d726..d7d9c4eac | `data/src/main/sqldelight/tachiyomi/data/merge_group.sq:34` | merge_group.sq twin markers reworded in range but still name no pin | The in-range edit changed comments only, and both queries sit behind one typed seam that pins them. |
| b47d2d726..d7d9c4eac | `eu/kanade/tachiyomi/ui/manga/MangaScreen.kt:446` | Genre tap walks back to any catalogue in the stack, whatever its source, and runs this series' genre there | The walk-back to the nearest catalogue is deliberate (MangaScreen.kt:405-432, commit 4c5efd3e4), and a sourceKey check would only turn the genre search into a text search on the same catalogue. |
| b47d2d726..d7d9c4eac | `reikai/presentation/recents/RecentsEngine.kt:210` | With the Downloaded filter on, the recents feed is rebuilt on every manga page-progress tick, which the guard says it avoids | The guard is the Downloaded-filter-off branch and it works; 96d9bd596 includes manga progress in the signal on purpose while the filter is on. |
| b47d2d726..d7d9c4eac | `docs/dev/plans/content-layer-reader-surface.md:397` | The reader surface's cutover section still says in present tense that DomainModule and MetroInteropModule survive | The line is a dated Status entry for step 9, true when written; the later removal of both modules is logged in upstream-sync.md:165. |
| b47d2d726..d7d9c4eac | `docs/dev/plans/content-layer-reader-surface.md:132` | The reader surface's step 4 names a completion constant that this range renamed | A historical step-log reference already resolved in the same bullet; the Approach section names the current CONTINUOUS_COMPLETE_PERCENT. |
| b47d2d726..d7d9c4eac | `test/java/reikai/presentation/reader/web/NovelDocumentGateTest.kt:33` | No gate test pins that opening a new document resets readiness, so deleting that reset leaves all seven tests green | The reset is pinned by the instrumented NovelWebViewportGateTest.aVerbSentWhileTheNextDocumentLoadsReachesThatDocument (NovelWebViewportGateTest.kt:96-106). |
| b47d2d726..d7d9c4eac | `androidTest/java/reikai/presentation/reader/NovelTextViewportWindowTest.kt:138` | The 'hiding a seam above the reader keeps their line' rule is a native/web twin pair added in range, not one contract case | Not a twin pair at one layer: the web test measures Chromium anchoring on the document, the native one the recycler layout manager, as the earlier row for this file ruled. |
| b47d2d726..d7d9c4eac | `eu/kanade/tachiyomi/ui/reader/ReaderViewModel.kt:754` | Manga download-ahead dedups with the next chapter as the tie-break, so it can queue a different scanlator's copy than the one the  | The next-chapter tie-break is upstream's own (refs/mihon ReaderViewModel.kt:519-521, same dedup rule), and the stated failure cannot occur since nextChapter comes from the reader's own list. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/NovelWebViewport.kt:459` | A 'run on append' JavaScript snippet runs twice or more for chapters added before the WebView page reports ready | The total run count matches the documented setting (one run per page plus one per added chapter) whether neighbours arrive before or after ready. |
| b47d2d726..d7d9c4eac | `reikai/presentation/reader/ReaderResume.kt:10` | A novel chapter marked read below 100% by the new threshold slider reopens at its start, losing the reader's place | Designed and tested: 9ce8a2472 added Resume reading position for exactly this case and device-checked it; the reopen-at-start behaviour predates the range. |
| f2251cb2e..b47d2d726 | `reikai/presentation/reader/NovelReaderViewModel.kt:570` | goTo picks the departed chapter after a suspension, so a second step can mark the chapter just opened read | A modal Loading dialog takes every touch and key while a load runs, so a second step can only land in the 10 to 20 ms before it shows, out of human reach. |
| f2251cb2e..b47d2d726 | `reikai/presentation/reader/text/NovelTextRenderer.kt:69` | A chapter's images-loading gate lifts before its re-measure lands, so the end check reads placeholder height | An end report only reads the last chapter, whose end is on screen only at the list end or when nothing scrolls; no onScrolled fires there, so only the post-remeasure layout check sees it. |
| f2251cb2e..b47d2d726 | `reikai/presentation/reader/NovelReaderViewModel.kt:882` | resolveGroupChapters' KDoc still says a preferred-source ranking picks the trunk, which the range removed | The KDoc says the preferred-source ranking picks the trunk, which is still true; the range moved that ranking to stitch-build time, it did not remove it. |
| f2251cb2e..b47d2d726 | `docs/dev/plans/content-layer-reader-surface.md:320` | New Status text keeps a tried-then-reverted narration and a watch item next to its own resolution | The cited lines are in the Status section, which roadmap-plans.md lets run as a dated log; the no-journey rule covers Approach only. |
| f2251cb2e..b47d2d726 | `test/java/reikai/presentation/reader/text/NovelWarmPolicyTest.kt:34` | The display name 'the cooldown clears itself on its last millisecond' puts the boundary on the wrong side | NovelWarmPolicy uses >=; the test pins 14999 ms denied and 15000 ms allowed, so the boundary and its name are right. |
| f2251cb2e..b47d2d726 | `androidTest/java/reikai/presentation/reader/WebViewSeamPositionTest.kt:363` | aChapterDroppedAboveMidScrollDoesNotMoveTheReader never checks that the eviction happened | A no-op or broken evictChapter is already caught by NovelWebDocumentTest.evictingAChapterTakesItsSeamWithIt, and one constant names the attribute on both sides. |
| f2251cb2e..b47d2d726 | `androidTest/java/reikai/presentation/reader/RecyclerPrependPositionTest.kt:490` | shortLastChapter_prependGrowsAfterLayout has no assertion and can only fail by throwing | The file is a measurement spike whose KDoc says assertions only pin conclusions; the grows-after case is a logged measurement by design. |
| f2251cb2e..b47d2d726 | `androidTest/java/reikai/presentation/reader/web/NovelWebDocumentTest.kt:107` | Three bridge-written collections in NovelWebDocumentTest are unsynchronized, so a negative assertion can pass  | steps is read after a latch; the other two are read after acquire points and a 500 ms settle and are written only when the answer changes, so neither failure occurs. |
| f2251cb2e..b47d2d726 | `reikai/presentation/reader/NovelTextViewport.kt:413` | New unpinned twin marker: NovelTextViewport.seekTo says 'The twin rejects it the same way' and names no pin | The rule is written once on the typed ReaderViewport.seekTo contract, whose KDoc binds both adapters; not an unpinned twin. |
| f2251cb2e..b47d2d726 | `reikai/presentation/reader/text/NovelTextStyle.kt:110` | The bundled-font typeface cache keeps the null-miss getOrPut shape 7597edbed removed from NovelFontManager | No writer of ln_reader_font_family can put a value into the bundled-font branch that misses, so the null re-lookup is unreachable. |
| f2251cb2e..b47d2d726 | `main/assets/novel-web/reader.js:472` | A chapter evicted and scrolled back into the window runs its scripts a second time | Ruled behaviour (content-layer-reader-surface.md, scripts in scrolled-in chapters): a re-added chapter is a fresh element whose scripts must run again; setting-on only. |
| f2251cb2e..b47d2d726 | `reikai/novel/content/NovelHtmlUtils.kt:282` | The WebView sanitizer's parser-exception fallback fails open, and runScripts would then run what it lets throu | No input was found that makes Jsoup.parseBodyFragment throw, so the fail-open fallback has no trigger. |
| f2251cb2e..b47d2d726 | `reikai/presentation/reader/NovelReaderViewModel.kt:586` | Two opens in flight commit in completion order, so the chapter asked for first can replace the one asked for l | load() raises the modal Loading dialog synchronously, which blocks the chapter list, steps, swipes and settings until it ends, so two opens cannot overlap. |
| f2251cb2e..b47d2d726 | `docs/dev/plans/merged-read-state.md:89` | merged-read-state.md Status and Approach still say the duplicate-read pass and helper use matchKey / a key fun | MergedChapterOrder does take a key function (keyOf), and the third claim is corrected later in the same record. |
| f2251cb2e..b47d2d726 | `docs/dev/plans/merged-read-state.md:57` | The 2026-08-12 note says recents verbs act on one chapter; the range made them group-wide | The paragraph is a dated Status entry; the same section records at line 63 that recents verbs now expand to the group. |
| 0f751005d..f2251cb2e | `eu/kanade/tachiyomi/ui/reader/ReaderActivity.kt:346` | A novel session locks the window to the global default orientation first and only switches to the novel's own override once it loa | The ordering the finding describes is real in code, but it is not a defect this range introduced, and the plan record rules the surface as carrying it unchanged. Mechanism, as read: `NovelReaderViewModel.kt:208` seeds `orientationOverride` at DEFAULT, `:612-618` fills it from an IO read in `init`, ` |
| 0f751005d..f2251cb2e | `eu/kanade/tachiyomi/ui/reader/ReaderViewModel.kt:970` | memberSourceNames is declared below the init block whose IO coroutine writes it, so its initializer can overwrite the value init() | javap on the built debug class (app/build/intermediates/built_in_kotlinc/debug/compileDebugKotlin/classes) settles both halves of the claim, and both go against it. Half one is bytecode-false. `customInfo`'s `= null` initializer emits no `putfield` in `<init>` at all: grepping the whole constructor |
| 0f751005d..f2251cb2e | `reikai/presentation/reader/NovelReaderProvider.kt:119` | NovelReaderProvider.createViewport KDoc says nothing rebuilds a novel viewport mid-session, but every Activity recreation does | "Mid-session" in this KDoc means "within one hosted Activity session", and in that sense it is true: grep shows createViewport is called only from ReaderActivity.kt:334 (the novel onCreate branch) and ReaderActivity.kt:994 (updateViewer, which ReaderActivity.kt:331-332 says never fires without a Man |
| 0f751005d..f2251cb2e | `reikai/presentation/novel/reader/NovelReaderScreenModel.kt:558` | The legacy chapter sheet spins forever if it is opened before the reading order has resolved | The mechanism is real as code (chapterRows at NovelReaderScreenModel.kt:557-567 captures chapterList() once, with no `if (orderedIds.isEmpty()) resolveReadingOrder()` guard like its twin at NovelReaderViewModel.kt:932, and ReaderChapterListDialog.kt:45-50 spins on empty rows), but both stated trigge |
| 0f751005d..f2251cb2e | `reikai/domain/novel/interactor/DeleteNovelChaptersBehindReader.kt:12` | New twin marker on DeleteNovelChaptersBehindReader names manga's deleteChapterIfNeeded with no pin, and only the novel half is tes | The twin is not new in range, a pin does exist for the contract, and the named failure cannot fire. 1. The manga/novel twin claim for this exact rule predates the range by two and a half months. `app/src/main/java/reikai/domain/novel/NovelPreferences.kt:357-359` already reads "Keep only the last N r |
| 0f751005d..f2251cb2e | `test/java/reikai/presentation/reader/ReaderEngineTest.kt:99` | 'a page action reaches the capability' only exercises the test's own fake and cannot fail on its own | Refuted: the finding is factually overstated and the gap it points at is answered by a ruled record. Two of its three claims survive scrutiny, one does not. The test at `app/src/test/java/reikai/presentation/reader/ReaderEngineTest.kt:99-105` does run production code: `ReaderEngine.openDialog` and t |
| 0f751005d..f2251cb2e | `test/java/reikai/presentation/reader/ReaderEngineTest.kt:402` | The engine's retry and bookmark passthroughs are untested, and the fake carries a retry counter no test reads | The facts check out but the defect does not. ReaderEngine.retryLoad() (ReaderEngine.kt:67) and toggleBookmark() (:73) are bare expression-body delegations with no engine-side logic, so the only possible assertion is a call-count on the fake, which .claude/rules/testing.md explicitly declines ("Don't |
| 0f751005d..f2251cb2e | `test/java/reikai/domain/novel/model/NovelReadingOrderTest.kt:31` | NovelReadingOrderTest's fixture helper inverts its 'descending' flag | The inverted polarity is real but inert. `readingOrderComparator` (app/src/main/java/reikai/domain/novel/model/NovelChapterFlags.kt:92-99) has no branch on the direction bit; it switches only on `effectiveSorting` and its KDoc at :85-88 states it is always ascending. The helper at NovelReadingOrderT |
| 0f751005d..f2251cb2e | `test/java/reikai/novel/content/NovelRegexReplacementsTest.kt:36` | The test documented as pinning the case-insensitive default passes caseSensitive explicitly, so flipping the stored-rule default s | The finding misreads which default the KDoc names. "The default" at NovelRegexReplacementsTest.kt:36 is the *processor* default, the project's own vocabulary for it: content-layer-reader-surface.md:258 criticises tsundoku because "their Test compiles the pattern with no options while the processor d |
| 0f751005d..f2251cb2e | `eu/kanade/presentation/more/settings/screen/SettingsMangaReaderScreen.kt:357` | Settings > Manga reader still shows nine Webtoon rows the high-quality renderer never reads; b165bf21a swapped only side padding | The factual half checks out: with `highQualityRenderer` on, `ReadingMode.toViewer` returns only `WebGpuViewer`/`WebGpuViewerContinuous` (app/src/main/java/eu/kanade/tachiyomi/ui/reader/setting/ReadingMode.kt:73-81), and `WebGpuConfig` reads `cropBorders`, `navigationModePager`, `pagerNavInverted`, ` |
| 0f751005d..f2251cb2e | `reikai/presentation/reader/NovelTextViewport.kt:225` | The native renderer reports position 0 for the anchor before its landing scroll, which the WebView renderer never does | The pre-landing report is plausible (NovelTextViewport.kt:800-805 posts `land` after `adapter.show`; ReaderActivity.kt:645-648 calls `rendererLanded` synchronously after `load` returns, so reports already count; the listener at NovelTextViewport.kt:221-227 reports unconditionally and ChapterScrollPr |
| 0f751005d..f2251cb2e | `reikai/presentation/reader/NovelWebViewport.kt:71` | Both WebView paths name the cutout inset `statusBarHeightPx` while the value passed in is dp | The chain is exactly as described, but the name does not contradict the value at the point it is consumed. `statusBarHeightPx` is a WebView-side parameter whose only consumer writes `--rk-inset-top:<n>px` into a CSS document (NovelWebDocument.kt:104), so its unit is the CSS pixel, which the document |
| 0f751005d..f2251cb2e | `docs/dev/plans/content-layer-reader-surface.md:37` | The reader plan doc's stated resolution for the surviving integer names a field that cannot exist | The second clause of line 37 is loose wording in a 2026-09-03 Approach bullet that later dated rulings in the same record already supersede, and the thing it promised is built and in use, so no session is left with an unbuildable deliverable. The typed position shipped as a kernel, not a field: `doc |
| 0f751005d..f2251cb2e | `eu/kanade/tachiyomi/ui/reader/ReaderViewModel.kt:419` | The RK ordering comment claims all three image viewers read requestedPage on every setChapters, which holds for only one | The island does not make the claim attributed to it. Its first clause scopes everything that follows to one moment: "this collector has to be subscribed before the first chapter arrives" (ReaderViewModel.kt:418). The "because" clause then explains that moment. The words "on every setChapters" are th |
| 0f751005d..f2251cb2e | `reikai/domain/novel/interactor/GetNextNovelChapter.kt:55` | Library continue-reading applies the entry's chapter filters on manga and ignores them on novels | The code difference is real but the finding's load-bearing premise ("no ruling in either plan doc") is false, and neither end of it was touched by the audited range. What I confirmed by reading: the manga library button does run the entry's filters (LibraryViewModel.kt:624-633 -> ChapterGetNextUnrea |
| 0f751005d..f2251cb2e | `eu/kanade/tachiyomi/ui/reader/ReaderActivity.kt:719` | onNewIntent compares the arriving launch against the launch intent and ignores source_scoped | The mechanism is described correctly but it is the ruled behaviour, not a defect. `ReaderActivity.kt:719-721` compares the arriving intent's entry id and `"chapter"` against `this.intent`, and `setIntent` appears exactly once in the tree, at :727 (executed: grep for `setIntent` across `app/src` retu |
| 0f751005d..f2251cb2e | `reikai/presentation/reader/NovelReaderTarget.kt:17` | Nothing pins the single-router rule that all three novel launches must go through novelReaderTarget | The finding's test-coverage facts check out, but the defect does not hold as stated, on three grounds. 1. The invariant holds at HEAD and there is no bypass to report. A whole-tree grep over `app/src` for both launch shapes returns exactly the declarations plus the router: `NovelReaderScreen` is con |
| 0f751005d..f2251cb2e | `test/java/reikai/presentation/reader/web/NovelWebBridgeTest.kt:46` | The only test of NovelWebBridge.onProgress passes against a deleted method body | The test is not vacuous for what it names, and the mechanism the finding fears is pinned next door. NovelWebBridgeTest's fixture gives the two gates different behaviour (fromDocument = { _, _ -> } at NovelWebBridgeTest.kt:12, fromReader = { _, call -> call() } at :13), so `a progress report waits fo |
| 0f751005d..f2251cb2e | `test/java/reikai/presentation/reader/web/NovelWebBridgeTest.kt:26` | Three bridge tests name a readiness property their fixture stubs out | The finding's facts hold but the defect does not. The three tests pin a real production clause rather than the property their fixture stipulates: with `fromDocument` stubbed to drop and `fromReader` to pass (NovelWebBridgeTest.kt:12-13), each assertion decides which of the two gates a method routes |
| 0f751005d..f2251cb2e | `androidTest/java/reikai/presentation/reader/NovelTextViewportWindowTest.kt:182` | "A reader inside a seam is in the chapter below it" is pinned by a hand-maintained twin pair, with the conformance harness for it | The cited lines are accurate (TextViewportContractTest.kt:69,:87 stub onVisibleChapter; the twin cases sit at NovelTextViewportWindowTest.kt:182 and NovelWebDocumentTest.kt:613 over two real implementations, NovelTextViewport.kt:749-755 and reader.js:99-106), but the rule invoked does not bind here |
| 0f751005d..f2251cb2e | `androidTest/java/reikai/presentation/reader/web/NovelWebDocumentTest.kt:824` | Three rules the parameterized contract test already runs against both viewports are still restated per renderer | The three "copies" are not copies of the contract test's cases; they sit one layer below it or carry a local job the contract test cannot do. Level, settled by grep: `grep -rn "NovelWebViewport(\/NovelTextViewport(" app/src/androidTest` returns only NovelTextViewportWindowTest.kt:79, NovelWebViewpor |
| 0f751005d..a6676a9de | `reikai/presentation/reader/NovelReaderViewModel.kt:947` | Progress writes once per whole percent and each write reruns the novel library query | Deliberate: the plan record (content-layer-reader-surface.md:289) ruled per-percent saves with a 500 ms debounce. |
| 0f751005d..a6676a9de | `reikai/presentation/reader/settings/NovelReaderSettingsPages.kt:124` | Novel sheet pages read and write NovelPreferences inside composables, bypassing ReaderTextSettings | Breaks nothing: both paths write the same preferences the provider observes, so no state diverges. |
| 0f751005d..a6676a9de | `docs/dev/plans/content-layer-reader-surface.md:80` | Menu-visibility ownership still "needs a ruling" while Status says the takeover is complete | The trap is conditional on moving menu visibility, which never moved, so no ruling was owed. |
| 0f751005d..a6676a9de | `test/java/reikai/presentation/reader/web/NovelWebBridgeTest.kt:44` | Bridge test names claim ready-gating but the gate is the test's own fake | The KDoc declares the fake; the tests pin which gate each bridge method routes through, which is production code. |
| 0f751005d..a6676a9de | `reikai/presentation/reader/NovelReaderViewModel.kt:1380` | Download-ahead re-downloads a merged chapter another source already has on disk | merged-read-state.md:72 deliberately leaves that case open, for manga and novels alike. |
| 0f751005d..a6676a9de | `main/assets/novel-web/reader.css:88` | Preformatted blocks in WebView mode do not wrap and are clipped | Recorded decision in the reader plan's WebView inventory: wide blocks clip rather than scroll the page sideways. |
| 0f751005d..a6676a9de | `reikai/presentation/reader/NovelReaderViewModel.kt:1277` | A fetch started before a text-setting change caches old-setting output after the clear | NovelChapterTextLoader.load reads the settings after the fetch returns, so an in-flight fetch builds with the new settings. |
