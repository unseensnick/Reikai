# Build files
.gradle
.kotlin
build

# IDE files
*.iml
.idea/*
!.idea/icon.svg
/captures

# Configuration files
local.properties

# Secrets (RK: never commit Firebase config or the release keystore)
google-services.json
app/google-services.json
keystore.properties

# macOS specific files
.DS_Store

# Claude tooling (local / globally-installed; not Reikai-specific)
.agents/
.claude/launch.json
.claude/skills/impeccable/
skills-lock.json

# Session handoff (local, transient)
Handoff.md

# Audit reports: local snapshots; only the folder's README is tracked (see docs/dev/audits/README.md)
docs/dev/audits/*
!docs/dev/audits/README.md

# README showcase: local scratch + regenerated intermediates (the reproducible kit lives in
# .github/readme-images/showcase/ ; see docs/dev/readme-showcase.md)
.github/readme-images/_capture/
.github/readme-images/showcase/framed/

# local dev tooling
.sweep-report.txt
scripts/plugin-sweep.ps1
